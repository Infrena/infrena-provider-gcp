<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.namespace`

Represents a namespace for a workload identity pool. Namespaces are used to segment identities within the pool.

[iam](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | iam |
| Scope | global |
| Asset type | `iam.googleapis.com/WorkloadIdentityPoolNamespace` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 1200s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | **no** | the create url `{+parent}/namespaces` needs `workloadIdentityPool`, which nothing supplies: no provider setting (project, region, zone, location), no stored binding, and no settable attribute of this type |
| Read | yes | `GET {+name}` |
| Update | yes | `PATCH {+name}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE {+name}` |
| Import | yes | by id, see below |

## Import id

```
{+name}
```

## Attributes

7 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `description` | — | `string` | optional | — | Optional. A description of the namespace. Cannot exceed 256 characters. |
| `disabled` | — | `boolean` | optional | — | Optional. Whether the namespace is disabled. If disabled, credentials may no longer be issued for identities within this namespace, however existing credentials will still be accepted until they … |
| `expireTime` | `expire_time` | `string` | output only | — | Output only. Time after which the namespace will be permanently purged and cannot be recovered. |
| `name` | — | `string` | optional | — | Identifier. The resource name of the namespace. |
| `ownerService` | `owner_service` | `map` | output only | — | Output only. The Google Cloud service that owns this namespace. |
| `ownerService.principalSubject` | `principal_subject` | `string` | optional | — | Required. The service agent principal subject, e.g. "serviceAccount:service-1234@gcp-sa-gkehub.iam.gserviceaccount.com". |
| `state` | — | `string` | output only | — | Output only. The state of the namespace. |
