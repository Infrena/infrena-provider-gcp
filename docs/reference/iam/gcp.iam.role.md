<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.iam.role`

A role in the Identity and Access Management API.

[iam](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | iam |
| Scope | global |
| Asset type | `iam.googleapis.com/Role` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 60s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/roles` |
| Read | yes | `GET {+name}` |
| Update | yes | `PATCH {+name}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE {+name}` |
| Import | yes | by id, see below |

## Import id

```
projects/{project}/roles/{role}
```

## Attributes

9 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `deleted` | — | `boolean` | optional | — | The current deleted state of the role. This field is read only. It will be ignored in calls to CreateRole and UpdateRole. |
| `description` | — | `string` | optional | — | Optional. A human-readable description for the role. |
| `etag` | — | `string` | optional | — | Used to perform a consistent read-modify-write. |
| `includedPermissions` | `included_permissions` | `list` | optional | — | The names of the permissions this role grants when bound in an IAM policy. |
| `includedPermissions[]` | — | `string` | optional | — | — |
| `name` | — | `string` | optional | — | The name of the role. When `Role` is used in `CreateRole`, the role name must not be set. When `Role` is used in output and other input such as `UpdateRole`, the role name is the complete path. For … |
| `roleId` | `role_id` | `string` | optional | force new, create only | The role ID to use for this role. A role ID may contain alphanumeric characters, underscores (`_`), and periods (`.`). It must contain a minimum of 3 characters and a maximum of 64 characters. |
| `stage` | — | `string` | optional | — | The current launch stage of the role. If the `ALPHA` launch stage has been selected for a role, the `stage` field will not be included in the returned definition for the role. |
| `title` | — | `string` | optional | — | Optional. A human-readable title for the role. Typically this is limited to 100 UTF-8 bytes. |
