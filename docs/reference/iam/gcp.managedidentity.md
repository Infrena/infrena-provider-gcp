<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.managedidentity`

Represents a managed identity for a workload identity pool namespace.

[iam](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | iam |
| Scope | global |
| Asset type | `iam.googleapis.com/WorkloadIdentityPoolManagedIdentity` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 1200s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | **no** | the create url `{+parent}/managedIdentities` needs `namespace`, `workloadIdentityPool`, which nothing supplies: no provider setting (project, region, zone, location), no stored binding, and no settable attribute of this type |
| Read | yes | `GET {+name}` |
| Update | **no** | publishes no update method; every change to it replaces the resource |
| Delete | yes | `DELETE {+name}` |
| Import | yes | by id, see below |

## Import id

```
{+name}
```

## Attributes

5 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `description` | — | `string` | optional | — | Optional. A description of the managed identity. Cannot exceed 256 characters. |
| `disabled` | — | `boolean` | optional | — | Optional. Whether the managed identity is disabled. If disabled, credentials may no longer be issued for the identity, however existing credentials will still be accepted until they expire. |
| `expireTime` | `expire_time` | `string` | output only | — | Output only. Time after which the managed identity will be permanently purged and cannot be recovered. |
| `name` | — | `string` | optional | — | Identifier. The resource name of the managed identity. |
| `state` | — | `string` | output only | — | Output only. The state of the managed identity. |
