<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.serviceaccount`

An IAM service account. A service account is an account for an application or a virtual machine (VM) instance, not a person. You can use a service account to call Google APIs. To learn more, read the \[overview of service accounts\](https://cloud.google.com/iam/help/service-accounts/overview). When you create a service account, you specify the project ID that owns the service account, as well as a name that must be unique within the project. IAM uses these values to create an email address that identifies the service account. //

[iam](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | iam |
| Scope | global |
| Asset type | `iam.googleapis.com/ServiceAccount` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 60s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/serviceAccounts` |
| Read | yes | `GET {+name}` |
| Update | yes | `PATCH {+name}` |
| Delete | yes | `DELETE {+name}` |
| Import | yes | by id, see below |

## Import id

```
{+name}
```

## Attributes

10 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `accountId` | `account_id` | `string` | optional | force new, create only | Required. The account id that is used to generate the service account email address and a stable unique id. It is unique within a project, must be 6-30 characters long, and match the regular … |
| `description` | — | `string` | optional | — | Optional. A user-specified, human-readable description of the service account. The maximum length is 256 UTF-8 bytes. |
| `disabled` | — | `boolean` | output only | — | Output only. Whether the service account is disabled. |
| `displayName` | `display_name` | `string` | optional | — | Optional. A user-specified, human-readable name for the service account. The maximum length is 100 UTF-8 bytes. |
| `email` | — | `string` | output only | — | Output only. The email address of the service account. |
| `etag` | — | `string` | optional | — | Deprecated. Do not use. |
| `name` | — | `string` | optional | — | The resource name of the service account. Use one of the following formats: * `projects/{PROJECT_ID}/serviceAccounts/{EMAIL_ADDRESS}` * `projects/{PROJECT_ID}/serviceAccounts/{UNIQUE_ID}` As an … |
| `oauth2ClientId` | `oauth2_client_id` | `string` | output only | — | Output only. The OAuth 2.0 client ID for the service account. |
| `projectId` | `project_id` | `string` | output only | — | Output only. The ID of the project that owns the service account. |
| `uniqueId` | `unique_id` | `string` | output only | — | Output only. The unique, stable numeric ID for the service account. Each service account retains its unique ID even if you delete the service account. For example, if you delete a service account … |
