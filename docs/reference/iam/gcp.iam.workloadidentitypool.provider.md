<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.iam.workloadidentitypool.provider`

A configuration for an external identity provider.

[iam](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | iam |
| Scope | global |
| Asset type | `iam.googleapis.com/WorkloadIdentityPoolProvider` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 1200s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | **no** | the create url `{+parent}/providers` needs `workloadIdentityPool`, which nothing supplies: no provider setting (project, region, zone, location), no stored binding, and no settable attribute of this type |
| Read | yes | `GET {+name}` |
| Update | yes | `PATCH {+name}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE {+name}` |
| Import | yes | by id, see below |

## Import id

```
{+name}
```

## Attributes

26 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `attributeCondition` | `attribute_condition` | `string` | optional | — | Optional. \[A Common Expression Language\](https://opensource.google/projects/cel) expression, in plain text, to restrict what otherwise valid authentication credentials issued by the provider should … |
| `attributeMapping` | `attribute_mapping` | `map` | optional | opaque | Optional. Maps attributes from authentication credentials issued by an external identity provider to Google Cloud attributes, such as `subject` and `segment`. Each key must be a string specifying the … |
| `aws` | — | `map` | optional | — | An Amazon Web Services identity provider. |
| `aws.accountId` | `account_id` | `string` | optional | — | Required. The AWS account ID. |
| `description` | — | `string` | optional | — | Optional. A description for the provider. Cannot exceed 256 characters. |
| `disabled` | — | `boolean` | optional | — | Optional. Whether the provider is disabled. You cannot use a disabled provider to exchange tokens. However, existing tokens still grant access. |
| `displayName` | `display_name` | `string` | optional | — | Optional. A display name for the provider. Cannot exceed 32 characters. |
| `expireTime` | `expire_time` | `string` | output only | — | Output only. Time after which the workload identity pool provider will be permanently purged and cannot be recovered. |
| `name` | — | `string` | optional | — | Identifier. The resource name of the provider. |
| `oidc` | — | `map` | optional | — | An OpenId Connect 1.0 identity provider. |
| `oidc.allowedAudiences` | `allowed_audiences` | `list` | optional | — | Optional. Acceptable values for the `aud` field (audience) in the OIDC token. Token exchange requests are rejected if the token audience does not match one of the configured values. Each audience may … |
| `oidc.allowedAudiences[]` | — | `string` | optional | — | — |
| `oidc.issuerUri` | `issuer_uri` | `string` | optional | — | Required. The OIDC `issuer_uri`. Must be an HTTPS endpoint. Per OpenID Connect Discovery 1.0 spec, the OIDC issuer URL is used to locate the provider's public keys (via `jwks_uri`) for verifying … |
| `oidc.jwksJson` | `jwks_json` | `string` | optional | — | Optional. OIDC JWKs in JSON String format. For details on the definition of a JWK, see https://tools.ietf.org/html/rfc7517. If not set, the `jwks_uri` from the discovery document(fetched from the … |
| `saml` | — | `map` | optional | — | An SAML 2.0 identity provider. |
| `saml.idpMetadataXml` | `idp_metadata_xml` | `string` | optional | — | Required. SAML identity provider (IdP) configuration metadata XML doc. The XML document must comply with the \[SAML 2.0 … |
| `state` | — | `string` | output only | — | Output only. The state of the provider. |
| `x509` | — | `map` | optional | — | An X.509-type identity provider. |
| `x509.trustStore` | `trust_store` | `map` | optional | — | Required. A TrustStore. Use this trust store as a wrapper to config the trust anchor and optional intermediate cas to help build the trust chain for the incoming end entity certificate. Follow the … |
| `x509.trustStore.intermediateCas` | `intermediate_cas` | `list` | optional | — | Optional. Set of intermediate CA certificates used for building the trust chain to the trust anchor. Important: Intermediate CAs are only supported for X.509 federation. |
| `x509.trustStore.intermediateCas[]` | — | `map` | optional | — | — |
| `x509.trustStore.intermediateCas[].pemCertificate` | `pem_certificate` | `string` | optional | — | PEM certificate of the PKI used for validation. Must only contain one ca certificate. |
| `x509.trustStore.trustAnchors` | `trust_anchors` | `list` | optional | — | Required. List of trust anchors to be used while performing validation against a given TrustStore. The incoming end entity's certificate must be in the trust chain of one of the trust anchors here. |
| `x509.trustStore.trustAnchors[]` | — | `map` | optional | — | — |
| `x509.trustStore.trustAnchors[].pemCertificate` | `pem_certificate` | `string` | optional | — | PEM certificate of the PKI used for validation. Must only contain one ca certificate (either root or intermediate cert). |
| `x509.trustStore.trustDefaultSharedCa` | `trust_default_shared_ca` | `boolean` | optional | — | Optional. Determines whether the trust bundle includes the private CA managed identity regional root public certificates. Important: `trust_default_shared_ca` is only supported for managed identity … |
