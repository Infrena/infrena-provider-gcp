<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.scimtenant`

Gemini Enterprise only. Represents a SCIM tenant. Used for provisioning and managing identity data (such as Users and Groups) in cross-domain environments.

[iam](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | iam |
| Scope | global |
| Asset type | `iam.googleapis.com/WorkforcePoolProviderScimTenant` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 60s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | **no** | the create url `{+parent}/scimTenants` needs `provider`, `workforcePool`, which nothing supplies: no provider setting (project, region, zone, location), no stored binding, and no settable attribute of this type |
| Read | yes | `GET {+name}` |
| Update | yes | `PATCH {+name}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE {+name}` |
| Import | yes | by id, see below |

## Import id

```
{+name}
```

## Attributes

8 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `baseUri` | `base_uri` | `string` | output only | — | Output only. Gemini Enterprise only. Represents the base URI as defined in \[RFC 7644, Section 1.3\](https://datatracker.ietf.org/doc/html/rfc7644#section-1.3). Clients must use this as the root … |
| `claimMapping` | `claim_mapping` | `map` | optional | opaque | Required. Immutable. Gemini Enterprise only. Maps SCIM attributes to Google attributes. This mapping is used to associate the attributes synced via SCIM with the Google Cloud attributes used in IAM … |
| `description` | — | `string` | optional | — | Optional. Gemini Enterprise only. The description of the SCIM tenant. Cannot exceed 256 characters. |
| `displayName` | `display_name` | `string` | optional | — | Optional. Gemini Enterprise only. The display name of the SCIM tenant. Cannot exceed 32 characters. |
| `name` | — | `string` | optional | — | Identifier. Gemini Enterprise only. The resource name of the SCIM Tenant. Format: `locations/{location}/workforcePools/{workforce_pool}/providers/ {workforce_pool_provider}/scimTenants/{scim_tenant}` |
| `purgeTime` | `purge_time` | `string` | output only | — | Output only. Gemini Enterprise only. The timestamp that represents the time when the SCIM tenant is purged. |
| `serviceAgent` | `service_agent` | `string` | output only | — | Output only. Service Agent created by SCIM Tenant API. SCIM tokens created under this tenant will be attached to this service agent. |
| `state` | — | `string` | output only | — | Output only. Gemini Enterprise only. The state of the tenant. |
