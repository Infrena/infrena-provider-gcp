<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.cloudresourcemanager.folder.folder`

A folder in an organization's resource hierarchy, used to organize that organization's resources.

[cloudresourcemanager](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | cloudresourcemanager |
| Scope | global |
| Asset type | `cloudresourcemanager.googleapis.com/Folder` |
| Hangs off | `folders` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 1200s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST folders` |
| Read | yes | `GET {+name}` |
| Update | yes | `PATCH {+name}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE {+name}` |
| Import | yes | by id, see below |

## Import id

```
{+name}
```

## Attributes

12 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `configuredCapabilities` | `configured_capabilities` | `list` | output only | — | Output only. Optional capabilities configured for this folder (via UpdateCapability API). Example: `folders/123/capabilities/app-management`. |
| `configuredCapabilities[]` | — | `string` | optional | — | — |
| `createTime` | `create_time` | `string` | output only | — | Output only. Timestamp when the folder was created. |
| `deleteTime` | `delete_time` | `string` | output only | — | Output only. Timestamp when the folder was requested to be deleted. |
| `displayName` | `display_name` | `string` | optional | — | The folder's display name. A folder's display name must be unique amongst its siblings. For example, no two folders with the same parent can share the same display name. The display name must start … |
| `etag` | — | `string` | output only | — | Output only. A checksum computed by the server based on the current value of the folder resource. This may be sent on update and delete requests to ensure the client has an up-to-date value before … |
| `managementProject` | `management_project` | `string` | output only | — | Output only. Management Project associated with this folder (if app-management capability is enabled). Example: `projects/google-mp-123` OUTPUT ONLY. |
| `name` | — | `string` | optional | — | Identifier. The resource name of the folder. Its format is `folders/{folder_id}`, for example: "folders/1234". |
| `parent` | — | `string` | optional | — | Required. The folder's parent's resource name. Updates to the folder's parent must be performed using MoveFolder. |
| `state` | — | `string` | output only | — | Output only. The lifecycle state of the folder. Updates to the state must be performed using DeleteFolder and UndeleteFolder. |
| `tags` | — | `map` | optional | force new, opaque | Optional. Input only. Immutable. Tag keys/values directly bound to this folder. Each item in the map must be expressed as " : ". For example: "123/environment" : "production", "123/costCenter" … |
| `updateTime` | `update_time` | `string` | output only | — | Output only. Timestamp when the folder was last modified. |
