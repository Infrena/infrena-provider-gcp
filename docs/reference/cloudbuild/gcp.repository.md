<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.repository`

A repository associated to a parent connection.

[cloudbuild](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | cloudbuild |
| Scope | regional |
| Asset type | `cloudbuild.googleapis.com/Repository` |
| Hangs off | `projects` |
| Tier | 1 (ruled: encoder (encoders/cloudbuildv2_repository.go.tmpl) derives project and location from Terraform's parent_connection field and writes the short name back. Nothing reaches the body; it is Terraform's own id bookkeeping.
) |
| Mutation timeout | 1200s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/locations/{{location}}/connections/{{parent_connection}}/repositories?repositoryId={{name}}` |
| Read | yes | `GET projects/{{project}}/locations/{{location}}/connections/{{parent_connection}}/repositories/{{name}}` |
| Update | **no** | publishes no update method; every change to it replaces the resource |
| Delete | yes | `DELETE projects/{{project}}/locations/{{location}}/connections/{{parent_connection}}/repositories/{{name}}` |
| Import | yes | by id, see below |

## Import id

```
projects/{{project}}/locations/{{location}}/connections/{{parent_connection}}/repositories/{{name}}
```

## Attributes

8 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `annotations` | — | `map` | optional | force new, opaque | Optional. Allows clients to store small amounts of arbitrary data. |
| `createTime` | `create_time` | `string` | output only | — | Output only. Server assigned timestamp for when the connection was created. |
| `etag` | — | `string` | output only | — | This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. |
| `name` | — | `string` | required | force new | Immutable. Resource name of the repository, in the format `projects/*/locations/*/connections/*/repositories/*`. |
| `parent_connection` | — | `string` | required | force new | The parent_connection this gcp.repository belongs to. It is part of the resource's name, supplied in the create url rather than in the request body. |
| `remoteUri` | `remote_uri` | `string` | required | force new | Required. Git Clone HTTPS URI. |
| `updateTime` | `update_time` | `string` | output only | — | Output only. Server assigned timestamp for when the connection was updated. |
| `webhookId` | `webhook_id` | `string` | output only | — | Output only. External ID of the webhook created for the repository. |
