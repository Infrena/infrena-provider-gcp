<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.spanner.backup`

A backup of a Cloud Spanner database.

[spanner](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | spanner |
| Scope | global |
| Asset type | `spanner.googleapis.com/Backup` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 1200s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | **no** | the create url `{+parent}/backups` needs `instance`, which nothing supplies: no provider setting (project, region, zone, location), no stored binding, and no settable attribute of this type |
| Read | yes | `GET {+name}` |
| Update | yes | `PATCH {+name}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE {+name}` |
| Import | yes | by id, see below |

## Import id

```
{+name}
```

## Attributes

36 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `backupSchedules` | `backup_schedules` | `list` | output only | — | Output only. List of backup schedule URIs that are associated with creating this backup. This is only applicable for scheduled backups, and is empty for on-demand backups. To optimize for storage … |
| `backupSchedules[]` | — | `string` | optional | — | — |
| `createTime` | `create_time` | `string` | output only | — | Output only. The time the CreateBackup request is received. If the request does not specify `version_time`, the `version_time` of the backup will be equivalent to the `create_time`. |
| `database` | — | `string` | optional | — | Required for the CreateBackup operation. Name of the database from which this backup was created. This needs to be in the same instance as the backup. Values are of the form … |
| `databaseDialect` | `database_dialect` | `string` | output only | — | Output only. The database dialect information for the backup. |
| `encryptionInfo` | `encryption_info` | `map` | output only | — | Output only. The encryption information for the backup. |
| `encryptionInfo.encryptionStatus` | `encryption_status` | `map` | output only | — | Output only. If present, the status of a recent encrypt/decrypt call on underlying data for this database or backup. Regardless of status, data is always encrypted at rest. |
| `encryptionInfo.encryptionStatus.code` | — | `integer` | optional | — | The status code, which should be an enum value of google.rpc.Code. |
| `encryptionInfo.encryptionStatus.details` | — | `list` | optional | — | A list of messages that carry the error details. There is a common set of message types for APIs to use. |
| `encryptionInfo.encryptionStatus.details[]` | — | `map` | optional | opaque | — |
| `encryptionInfo.encryptionStatus.message` | — | `string` | optional | — | A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client. |
| `encryptionInfo.encryptionType` | `encryption_type` | `string` | output only | — | Output only. The type of encryption. |
| `encryptionInfo.kmsKeyVersion` | `kms_key_version` | `string` | output only | — | Output only. A Cloud KMS key version that is being used to protect the database or backup. |
| `encryptionInformation` | `encryption_information` | `list` | output only | — | Output only. The encryption information for the backup, whether it is protected by one or more KMS keys. The information includes all Cloud KMS key versions used to encrypt the backup. The … |
| `encryptionInformation[]` | — | `map` | optional | — | — |
| `encryptionInformation[].encryptionStatus` | `encryption_status` | `map` | output only | same fields as `encryptionInfo.encryptionStatus` | Output only. If present, the status of a recent encrypt/decrypt call on underlying data for this database or backup. Regardless of status, data is always encrypted at rest. |
| `encryptionInformation[].encryptionType` | `encryption_type` | `string` | output only | — | Output only. The type of encryption. |
| `encryptionInformation[].kmsKeyVersion` | `kms_key_version` | `string` | output only | — | Output only. A Cloud KMS key version that is being used to protect the database or backup. |
| `exclusiveSizeBytes` | `exclusive_size_bytes` | `string` | output only | — | Output only. For a backup in an incremental backup chain, this is the storage space needed to keep the data that has changed since the previous backup. For all other backups, this is always the size … |
| `expireTime` | `expire_time` | `string` | optional | — | Required for the CreateBackup operation. The expiration time of the backup, with microseconds granularity that must be at least 6 hours and at most 366 days from the time the CreateBackup request is … |
| `freeableSizeBytes` | `freeable_size_bytes` | `string` | output only | — | Output only. The number of bytes that will be freed by deleting this backup. This value will be zero if, for example, this backup is part of an incremental backup chain and younger backups in the … |
| `incrementalBackupChainId` | `incremental_backup_chain_id` | `string` | output only | — | Output only. Populated only for backups in an incremental backup chain. Backups share the same chain id if and only if they belong to the same incremental backup chain. Use this field to determine … |
| `instancePartitions` | `instance_partitions` | `list` | output only | — | Output only. The instance partition storing the backup. This is the same as the list of the instance partitions that the database recorded at the backup's `version_time`. |
| `instancePartitions[]` | — | `map` | optional | — | — |
| `instancePartitions[].instancePartition` | `instance_partition` | `string` | optional | — | A unique identifier for the instance partition. Values are of the form `projects/{project}/instances/{instance}/instancePartitions/{instance_partition_id}` |
| `maxExpireTime` | `max_expire_time` | `string` | output only | — | Output only. The max allowed expiration time of the backup, with microseconds granularity. A backup's expiration time can be configured in multiple APIs: CreateBackup, UpdateBackup, CopyBackup. When … |
| `minimumRestorableEdition` | `minimum_restorable_edition` | `string` | output only | — | Output only. The minimum edition required to successfully restore the backup. Populated only if the edition is Enterprise or Enterprise Plus. |
| `name` | — | `string` | optional | — | Output only for the CreateBackup operation. Required for the UpdateBackup operation. A globally unique identifier for the backup which cannot be changed. Values are of the form … |
| `oldestVersionTime` | `oldest_version_time` | `string` | output only | — | Output only. Data deleted at a time older than this is guaranteed not to be retained in order to support this backup. For a backup in an incremental backup chain, this is the version time of the … |
| `referencingBackups` | `referencing_backups` | `list` | output only | — | Output only. The names of the destination backups being created by copying this source backup. The backup names are of the form `projects/{project}/instances/{instance}/backups/{backup}`. Referencing … |
| `referencingBackups[]` | — | `string` | optional | — | — |
| `referencingDatabases` | `referencing_databases` | `list` | output only | — | Output only. The names of the restored databases that reference the backup. The database names are of the form `projects/{project}/instances/{instance}/databases/{database}`. Referencing databases … |
| `referencingDatabases[]` | — | `string` | optional | — | — |
| `sizeBytes` | `size_bytes` | `string` | output only | — | Output only. Size of the backup in bytes. For a backup in an incremental backup chain, this is the sum of the `exclusive_size_bytes` of itself and all older backups in the chain. |
| `state` | — | `string` | output only | — | Output only. The current state of the backup. |
| `versionTime` | `version_time` | `string` | optional | — | The backup will contain an externally consistent copy of the database at the timestamp specified by `version_time`. If `version_time` is not specified, the system will set `version_time` to the … |
