<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.redis.instance`

A Google Cloud Redis instance.

[redis](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | redis |
| Scope | regional |
| Asset type | `redis.googleapis.com/Instance` |
| Hangs off | `projects` |
| Tier | 1 (ruled: encoder (encoders/redis_location_id_for_fallback_zone.go.tmpl) only derives Terraform's region from locationId and returns the body unchanged. decoder (decoders/redis_instance.go.tmpl) GETs authString into Terraform's own auth_string field; here read it with getAuthString. pre_delete (pre_delete/redis_instance.go.tmpl) refuses when Terraform's deletion_protection is set; the v1 Instance has no such field.
THE BOUNDED GAP, from magic-modules' constants rather than a hook: Terraform suppresses the diff when secondaryIpRange is "auto". Here writing "auto" plans an update on every run; omit it, or write the range Google chose.
) |
| Mutation timeout | 1200s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/locations/{{region}}/instances?instanceId={{name}}` |
| Read | yes | `GET {+name}` |
| Update | yes | `PATCH {+name}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE {+name}` |
| Import | yes | by id, see below |

## Import id

```
projects/{project}/locations/{location}/instances/{instance}
```

## Attributes

69 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `alternativeLocationId` | `alternative_location_id` | `string` | optional | force new | Optional. If specified, at least one node will be provisioned in this zone in addition to the zone specified in location_id. Only applicable to standard tier. If provided, it must be a different zone … |
| `authEnabled` | `auth_enabled` | `boolean` | optional | — | Optional. Indicates whether OSS Redis AUTH is enabled for the instance. If set to "true" AUTH is enabled on the instance. Default value is "false" meaning AUTH is disabled. |
| `authorizedNetwork` | `authorized_network` | `string` | optional | force new | Optional. The full name of the Google Compute Engine \[network\](https://cloud.google.com/vpc/docs/vpc) to which the instance is connected. If left unspecified, the `default` network will be used. |
| `availableMaintenanceVersions` | `available_maintenance_versions` | `list` | optional | — | Optional. The available maintenance versions that an instance could update to. |
| `availableMaintenanceVersions[]` | — | `string` | optional | — | — |
| `connectMode` | `connect_mode` | `string` | optional | force new | Optional. The network connect mode of the Redis instance. If not provided, the connect mode defaults to DIRECT_PEERING. |
| `createTime` | `create_time` | `string` | output only | — | Output only. The time the instance was created. |
| `currentLocationId` | `current_location_id` | `string` | output only | — | Output only. The current zone where the Redis primary node is located. In basic tier, this will always be the same as \[location_id\]. In standard tier, this can be the zone of any node in the … |
| `customerManagedKey` | `customer_managed_key` | `string` | optional | force new | Optional. The KMS key reference that the customer provides when trying to create the instance. |
| `displayName` | `display_name` | `string` | optional | — | An arbitrary and optional user-provided name for the instance. |
| `host` | — | `string` | output only | — | Output only. Hostname or IP address of the exposed Redis endpoint used by clients to connect to the service. |
| `labels` | — | `map` | optional | opaque | Resource labels to represent user provided metadata |
| `locationId` | `location_id` | `string` | optional | force new | Optional. The zone where the instance will be provisioned. If not provided, the service will choose a zone from the specified region for the instance. For standard tier, additional nodes will be … |
| `maintenancePolicy` | `maintenance_policy` | `map` | optional | — | Optional. The maintenance policy for the instance. If not provided, maintenance events can be performed at any time. |
| `maintenancePolicy.createTime` | `create_time` | `string` | output only | — | Output only. The time when the policy was created. |
| `maintenancePolicy.description` | — | `string` | optional | — | Optional. Description of what this policy is for. Create/Update methods return INVALID_ARGUMENT if the length is greater than 512. |
| `maintenancePolicy.updateTime` | `update_time` | `string` | output only | — | Output only. The time when the policy was last updated. |
| `maintenancePolicy.weeklyMaintenanceWindow` | `weekly_maintenance_window` | `list` | optional | — | Optional. Maintenance window that is applied to resources covered by this policy. Minimum 1. For the current version, the maximum number of weekly_window is expected to be one. |
| `maintenancePolicy.weeklyMaintenanceWindow[]` | — | `map` | optional | — | — |
| `maintenancePolicy.weeklyMaintenanceWindow[].day` | — | `string` | required | — | Required. The day of week that maintenance updates occur. |
| `maintenancePolicy.weeklyMaintenanceWindow[].duration` | — | `string` | output only | — | Output only. Duration of the maintenance window. The current window is fixed at 1 hour. |
| `maintenancePolicy.weeklyMaintenanceWindow[].startTime` | `start_time` | `map` | required | — | Required. Start time of the window in UTC time. |
| `maintenancePolicy.weeklyMaintenanceWindow[].startTime.hours` | — | `integer` | optional | — | Hours of a day in 24 hour format. Must be greater than or equal to 0 and typically must be less than or equal to 23. An API may choose to allow the value "24:00:00" for scenarios like business … |
| `maintenancePolicy.weeklyMaintenanceWindow[].startTime.minutes` | — | `integer` | optional | — | Minutes of an hour. Must be greater than or equal to 0 and less than or equal to 59. |
| `maintenancePolicy.weeklyMaintenanceWindow[].startTime.nanos` | — | `integer` | optional | — | Fractions of seconds, in nanoseconds. Must be greater than or equal to 0 and less than or equal to 999,999,999. |
| `maintenancePolicy.weeklyMaintenanceWindow[].startTime.seconds` | — | `integer` | optional | — | Seconds of a minute. Must be greater than or equal to 0 and typically must be less than or equal to 59. An API may allow the value 60 if it allows leap-seconds. |
| `maintenanceSchedule` | `maintenance_schedule` | `map` | output only | — | Output only. Date and time of upcoming maintenance events which have been scheduled. |
| `maintenanceSchedule.canReschedule` | `can_reschedule` | `boolean` | optional | — | If the scheduled maintenance can be rescheduled, default is true. |
| `maintenanceSchedule.endTime` | `end_time` | `string` | output only | — | Output only. The end time of any upcoming scheduled maintenance for this instance. |
| `maintenanceSchedule.scheduleDeadlineTime` | `schedule_deadline_time` | `string` | output only | — | Output only. The deadline that the maintenance schedule start time can not go beyond, including reschedule. |
| `maintenanceSchedule.startTime` | `start_time` | `string` | output only | — | Output only. The start time of any upcoming scheduled maintenance for this instance. |
| `maintenanceVersion` | `maintenance_version` | `string` | optional | — | Optional. The self service update maintenance version. The version is date based such as "20210712_00_00". |
| `memorySizeGb` | `memory_size_gb` | `integer` | required | — | Required. Redis memory size in GiB. |
| `name` | — | `string` | required | force new | Required. Unique name of the resource in this scope including project and location using the form: `projects/{project_id}/locations/{location_id}/instances/{instance_id}` Note: Redis instances are … |
| `nodes` | — | `list` | output only | — | Output only. Info per node. |
| `nodes[]` | — | `map` | optional | — | — |
| `nodes[].id` | — | `string` | output only | — | Output only. Node identifying string. e.g. 'node-0', 'node-1' |
| `nodes[].zone` | — | `string` | output only | — | Output only. Location of the node. |
| `persistenceConfig` | `persistence_config` | `map` | optional | — | Optional. Persistence configuration parameters |
| `persistenceConfig.persistenceMode` | `persistence_mode` | `string` | required | — | Optional. Controls whether Persistence features are enabled. If not provided, the existing value will be used. |
| `persistenceConfig.rdbNextSnapshotTime` | `rdb_next_snapshot_time` | `string` | output only | — | Output only. The next time that a snapshot attempt is scheduled to occur. |
| `persistenceConfig.rdbSnapshotPeriod` | `rdb_snapshot_period` | `string` | optional | — | Optional. Period between RDB snapshots. Snapshots will be attempted every period starting from the provided snapshot start time. For example, a start time of 01/01/2033 06:45 and SIX_HOURS snapshot … |
| `persistenceConfig.rdbSnapshotStartTime` | `rdb_snapshot_start_time` | `string` | optional | — | Optional. Date and time that the first snapshot was/will be attempted, and to which future snapshots will be aligned. If not provided, the current time will be used. |
| `persistenceIamIdentity` | `persistence_iam_identity` | `string` | output only | — | Output only. Cloud IAM identity used by import / export operations to transfer data to/from Cloud Storage. Format is "serviceAccount:". The value may change over time for a given instance so should … |
| `port` | — | `integer` | output only | — | Output only. The port number of the exposed Redis endpoint. |
| `readEndpoint` | `read_endpoint` | `string` | output only | — | Output only. Hostname or IP address of the exposed readonly Redis endpoint. Standard tier only. Targets all healthy replica nodes in instance. Replication is asynchronous and replica nodes will … |
| `readEndpointPort` | `read_endpoint_port` | `integer` | output only | — | Output only. The port number of the exposed readonly redis endpoint. Standard tier only. Write requests should target 'port'. |
| `readReplicasMode` | `read_replicas_mode` | `string` | optional | — | Optional. Read replicas mode for the instance. Defaults to READ_REPLICAS_DISABLED. |
| `redisConfigs` | `redis_configs` | `map` | optional | opaque | Optional. Redis configuration parameters, according to \[Redis configuration\](https://redis.io/docs/latest/operate/oss_and_stack/management/config/). Currently, the only supported parameters are … |
| `redisVersion` | `redis_version` | `string` | optional | — | Optional. The version of Redis software. If not provided, the default version will be used. Currently, the supported values are: * `REDIS_3_2` for Redis 3.2 compatibility * `REDIS_4_0` for Redis 4.0 … |
| `replicaCount` | `replica_count` | `integer` | optional | — | Optional. The number of replica nodes. The valid range for the Standard Tier with read replicas enabled is \[1-5\] and defaults to 2. If read replicas are not enabled for a Standard Tier instance … |
| `reservedIpRange` | `reserved_ip_range` | `string` | optional | force new | Optional. For DIRECT_PEERING mode, the CIDR range of internal addresses that are reserved for this instance. Range must be unique and non-overlapping with existing subnets in an authorized network … |
| `satisfiesPzi` | `satisfies_pzi` | `boolean` | output only | — | Optional. Output only. Reserved for future use. |
| `satisfiesPzs` | `satisfies_pzs` | `boolean` | output only | — | Optional. Output only. Reserved for future use. |
| `secondaryIpRange` | `secondary_ip_range` | `string` | optional | — | Optional. Additional IP range for node placement. Required when enabling read replicas on an existing instance. For DIRECT_PEERING mode value must be a CIDR range of size /28, or "auto". For … |
| `serverCaCerts` | `server_ca_certs` | `list` | output only | — | Output only. List of server CA certificates for the instance. |
| `serverCaCerts[]` | — | `map` | optional | — | — |
| `serverCaCerts[].cert` | — | `string` | output only | — | PEM representation. |
| `serverCaCerts[].createTime` | `create_time` | `string` | output only | — | Output only. The time when the certificate was created in \[RFC 3339\](https://tools.ietf.org/html/rfc3339) format, for example `2020-05-18T00:00:00.094Z`. |
| `serverCaCerts[].expireTime` | `expire_time` | `string` | output only | — | Output only. The time when the certificate expires in \[RFC 3339\](https://tools.ietf.org/html/rfc3339) format, for example `2020-05-18T00:00:00.094Z`. |
| `serverCaCerts[].serialNumber` | `serial_number` | `string` | output only | — | Serial number, as extracted from the certificate. |
| `serverCaCerts[].sha1Fingerprint` | `sha1_fingerprint` | `string` | output only | — | Sha1 Fingerprint of the certificate. |
| `state` | — | `string` | output only | — | Output only. The current state of this instance. |
| `statusMessage` | `status_message` | `string` | output only | — | Output only. Additional information about the current status of this instance, if available. |
| `suspensionReasons` | `suspension_reasons` | `list` | optional | — | Optional. reasons that causes instance in "SUSPENDED" state. |
| `suspensionReasons[]` | — | `string` | optional | — | — |
| `tags` | — | `map` | optional | force new, opaque | Optional. Input only. Immutable. Tag keys/values directly bound to this resource. For example: "123/environment": "production", "123/costCenter": "marketing" |
| `tier` | — | `string` | optional | force new | Required. The service tier of the instance. |
| `transitEncryptionMode` | `transit_encryption_mode` | `string` | optional | force new | Optional. The TLS mode of the Redis instance. If not provided, TLS is disabled for the instance. |
