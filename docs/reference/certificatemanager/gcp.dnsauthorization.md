<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.dnsauthorization`

DnsAuthorization represents a HTTP-reachable backend for a DnsAuthorization.

[certificatemanager](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | certificatemanager |
| Scope | regional |
| Asset type | `certificatemanager.googleapis.com/DnsAuthorization` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 1200s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/locations/{{location}}/dnsAuthorizations?dnsAuthorizationId={{name}}` |
| Read | yes | `GET projects/{{project}}/locations/{{location}}/dnsAuthorizations/{{name}}` |
| Update | yes | `PATCH projects/{{project}}/locations/{{location}}/dnsAuthorizations/{{name}}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE projects/{{project}}/locations/{{location}}/dnsAuthorizations/{{name}}` |
| Import | yes | by id, see below |

## Import id

```
projects/{{project}}/locations/{{location}}/dnsAuthorizations/{{name}}
```

## Attributes

12 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `createTime` | `create_time` | `string` | output only | — | Output only. The creation timestamp of a DnsAuthorization. |
| `description` | — | `string` | optional | — | Optional. One or more paragraphs of text description of a DnsAuthorization. |
| `dnsResourceRecord` | `dns_resource_record` | `map` | output only | — | Output only. DNS Resource Record that needs to be added to DNS configuration. |
| `dnsResourceRecord.data` | — | `string` | output only | — | Output only. Data of the DNS Resource Record. |
| `dnsResourceRecord.name` | — | `string` | output only | force new | Output only. Fully qualified name of the DNS Resource Record. e.g. `_acme-challenge.example.com` |
| `dnsResourceRecord.type_value` | wire `type` | `string` | output only | force new | Output only. Type of the DNS Resource Record. Currently always set to "CNAME". |
| `domain` | — | `string` | required | force new | Required. Immutable. A domain that is being authorized. A DnsAuthorization resource covers a single domain and its wildcard, e.g. authorization for `example.com` can be used to issue certificates for … |
| `labels` | — | `map` | optional | opaque | Optional. Set of labels associated with a DnsAuthorization. |
| `name` | — | `string` | required | force new | Identifier. A user-defined name of the dns authorization. DnsAuthorization names must be unique globally and match pattern `projects/*/locations/*/dnsAuthorizations/*`. |
| `tags` | — | `map` | optional | force new, opaque | Optional. Input only. Immutable. Tag keys/values directly bound to this resource. For example: "123/environment": "production", "123/costCenter": "marketing" |
| `type_value` | wire `type` | `string` | optional | force new | Optional. Immutable. Type of DnsAuthorization. If unset during resource creation the following default will be used: - in location `global`: FIXED_RECORD, - in other locations: PER_PROJECT_RECORD. |
| `updateTime` | `update_time` | `string` | output only | — | Output only. The last update timestamp of a DnsAuthorization. |
