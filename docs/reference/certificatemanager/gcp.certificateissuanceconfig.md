<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.certificateissuanceconfig`

Certificate represents a HTTP-reachable backend for a Certificate.

[certificatemanager](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | certificatemanager |
| Scope | regional |
| Asset type | `certificatemanager.googleapis.com/CertificateIssuanceConfig` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 1200s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/locations/{{location}}/certificateIssuanceConfigs?certificateIssuanceConfigId={{name}}` |
| Read | yes | `GET {+name}` |
| Update | yes | `PATCH {+name}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE {+name}` |
| Import | yes | by id, see below |

## Import id

```
projects/{{project}}/locations/{{location}}/certificateIssuanceConfigs/{{name}}
```

## Attributes

12 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `certificateAuthorityConfig` | `certificate_authority_config` | `map` | required | — | Required. The CA that issues the workload certificate. It includes the CA address, type, authentication to CA service, etc. |
| `certificateAuthorityConfig.certificateAuthorityServiceConfig` | `certificate_authority_service_config` | `map` | optional | — | Defines a CertificateAuthorityServiceConfig. |
| `certificateAuthorityConfig.certificateAuthorityServiceConfig.caPool` | `ca_pool` | `string` | required | — | Required. A CA pool resource used to issue a certificate. The CA pool string has a relative resource path following the form "projects/{project}/locations/{location}/caPools/{ca_pool}". |
| `createTime` | `create_time` | `string` | output only | — | Output only. The creation timestamp of a CertificateIssuanceConfig. |
| `description` | — | `string` | optional | — | Optional. One or more paragraphs of text description of a CertificateIssuanceConfig. |
| `keyAlgorithm` | `key_algorithm` | `string` | required | — | Required. The key algorithm to use when generating the private key. |
| `labels` | — | `map` | optional | opaque | Optional. Set of labels associated with a CertificateIssuanceConfig. |
| `lifetime` | — | `string` | required | — | Required. Workload certificate lifetime requested. |
| `name` | — | `string` | required | — | Identifier. A user-defined name of the certificate issuance config. CertificateIssuanceConfig names must be unique globally and match pattern `projects/*/locations/*/certificateIssuanceConfigs/*`. |
| `rotationWindowPercentage` | `rotation_window_percentage` | `integer` | required | — | Required. Specifies the percentage of elapsed time of the certificate lifetime to wait before renewing the certificate. Must be a number between 1-99, inclusive. |
| `tags` | — | `map` | optional | opaque | Optional. Input only. Immutable. Tag keys/values directly bound to this resource. For example: "123/environment": "production", "123/costCenter": "marketing" |
| `updateTime` | `update_time` | `string` | output only | — | Output only. The last update timestamp of a CertificateIssuanceConfig. |
