<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.certificatemapentry`

CertificateMapEntry is a list of certificate configurations, that have been issued for a particular hostname

[certificatemanager](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | certificatemanager |
| Scope | regional |
| Asset type | `certificatemanager.googleapis.com/CertificateMapEntry` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 1200s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/locations/global/certificateMaps/{{map}}/certificateMapEntries?certificateMapEntryId={{name}}` |
| Read | yes | `GET projects/{{project}}/locations/global/certificateMaps/{{map}}/certificateMapEntries/{{name}}` |
| Update | yes | `PATCH projects/{{project}}/locations/global/certificateMaps/{{map}}/certificateMapEntries/{{name}}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE projects/{{project}}/locations/global/certificateMaps/{{map}}/certificateMapEntries/{{name}}` |
| Import | yes | by id, see below |

## Import id

```
projects/{{project}}/locations/global/certificateMaps/{{map}}/certificateMapEntries/{{name}}
```

## Attributes

11 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `certificates` | — | `list` | required | — | Optional. A set of Certificates defines for the given `hostname`. There can be defined up to four certificates in each Certificate Map Entry. Each certificate must match pattern … |
| `certificates[]` | — | `string` | optional | — | — |
| `createTime` | `create_time` | `string` | output only | — | Output only. The creation timestamp of a Certificate Map Entry. |
| `description` | — | `string` | optional | — | Optional. One or more paragraphs of text description of a certificate map entry. |
| `hostname` | — | `string` | optional | force new | A Hostname (FQDN, e.g. `example.com`) or a wildcard hostname expression (`*.example.com`) for a set of hostnames with common suffix. Used as Server Name Indication (SNI) for selecting a proper … |
| `labels` | — | `map` | optional | opaque | Optional. Set of labels associated with a Certificate Map Entry. |
| `map` | — | `string` | required | force new | The map this gcp.certificatemapentry belongs to. It is part of the resource's name, supplied in the create url rather than in the request body. |
| `matcher` | — | `string` | optional | force new | A predefined matcher for particular cases, other than SNI selection. |
| `name` | — | `string` | required | force new | Identifier. A user-defined name of the Certificate Map Entry. Certificate Map Entry names must be unique globally and match pattern `projects/*/locations/*/certificateMaps/*/certificateMapEntries/*`. |
| `state` | — | `string` | output only | — | Output only. A serving state of this Certificate Map Entry. |
| `updateTime` | `update_time` | `string` | output only | — | Output only. The update timestamp of a Certificate Map Entry. |
