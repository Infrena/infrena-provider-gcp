<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.function`

Describes a Cloud Function that contains user computation executed in response to an event. It encapsulates function and trigger configurations.

[cloudfunctions](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | cloudfunctions |
| Scope | global |
| Asset type | `cloudfunctions.googleapis.com/Function` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 1200s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/locations/{{location}}/functions?functionId={{functionId}}` |
| Read | yes | `GET {+name}` |
| Update | yes | `PATCH {+name}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE {+name}` |
| Import | yes | by id, see below |

## Import id

```
projects/{project}/locations/{location}/functions/{function}
```

## Attributes

160 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `buildConfig` | `build_config` | `map` | optional | — | Describes the Build step of the function that builds a container from the given source. |
| `buildConfig.automaticUpdatePolicy` | `automatic_update_policy` | `map` | optional | opaque | Security patches are applied automatically to the runtime without requiring the function to be redeployed. |
| `buildConfig.build` | — | `string` | output only | — | Output only. The Cloud Build name of the latest successful deployment of the function. |
| `buildConfig.dockerRegistry` | `docker_registry` | `string` | optional | — | Docker Registry to use for this deployment. This configuration is only applicable to 1st Gen functions, 2nd Gen functions can only use Artifact Registry. Deprecated: as of March 2025 … |
| `buildConfig.dockerRepository` | `docker_repository` | `string` | optional | — | Repository in Artifact Registry to which the function docker image will be pushed after it is built by Cloud Build. If specified by user, it is created and managed by user with a customer managed … |
| `buildConfig.entryPoint` | `entry_point` | `string` | optional | — | The name of the function (as defined in source code) that will be executed. Defaults to the resource name suffix, if not specified. For backward compatibility, if function with given name is not … |
| `buildConfig.environmentVariables` | `environment_variables` | `map` | optional | opaque | User-provided build-time environment variables for the function |
| `buildConfig.onDeployUpdatePolicy` | `on_deploy_update_policy` | `map` | optional | — | Security patches are only applied when a function is redeployed. |
| `buildConfig.onDeployUpdatePolicy.runtimeVersion` | `runtime_version` | `string` | output only | — | Output only. contains the runtime version which was used during latest function deployment. |
| `buildConfig.runtime` | — | `string` | optional | — | The runtime in which to run the function. Required when deploying a new function, optional when updating an existing function. For a complete list of possible choices, see the \[`gcloud` command … |
| `buildConfig.serviceAccount` | `service_account` | `string` | optional | — | Service account to be used for building the container. The format of this field is `projects/{projectId}/serviceAccounts/{serviceAccountEmail}`. |
| `buildConfig.source` | — | `map` | optional | — | The location of the function source code. |
| `buildConfig.source.gitUri` | `git_uri` | `string` | optional | — | If provided, get the source from GitHub repository. This option is valid only for GCF 1st Gen function. Example: https://github.com///blob// |
| `buildConfig.source.repoSource` | `repo_source` | `map` | optional | — | If provided, get the source from this location in a Cloud Source Repository. |
| `buildConfig.source.repoSource.branchName` | `branch_name` | `string` | optional | — | Regex matching branches to build. The syntax of the regular expressions accepted is the syntax accepted by RE2 and described at https://github.com/google/re2/wiki/Syntax |
| `buildConfig.source.repoSource.commitSha` | `commit_sha` | `string` | optional | — | Explicit commit SHA to build. |
| `buildConfig.source.repoSource.dir` | — | `string` | optional | — | Directory, relative to the source root, in which to run the build. This must be a relative path. If a step's `dir` is specified and is an absolute path, this value is ignored for that step's … |
| `buildConfig.source.repoSource.projectId` | `project_id` | `string` | optional | — | ID of the project that owns the Cloud Source Repository. If omitted, the project ID requesting the build is assumed. |
| `buildConfig.source.repoSource.repoName` | `repo_name` | `string` | optional | — | Name of the Cloud Source Repository. |
| `buildConfig.source.repoSource.tagName` | `tag_name` | `string` | optional | — | Regex matching tags to build. The syntax of the regular expressions accepted is the syntax accepted by RE2 and described at https://github.com/google/re2/wiki/Syntax |
| `buildConfig.source.storageSource` | `storage_source` | `map` | optional | — | If provided, get the source from this location in Google Cloud Storage. |
| `buildConfig.source.storageSource.bucket` | — | `string` | optional | — | Google Cloud Storage bucket containing the source (see \[Bucket Name Requirements\](https://cloud.google.com/storage/docs/bucket-naming#requirements)). |
| `buildConfig.source.storageSource.generation` | — | `string` | optional | — | Google Cloud Storage generation for the object. If the generation is omitted, the latest generation will be used. |
| `buildConfig.source.storageSource.object` | — | `string` | optional | — | Google Cloud Storage object containing the source. This object must be a gzipped archive file (`.tar.gz`) containing source to build. |
| `buildConfig.source.storageSource.sourceUploadUrl` | `source_upload_url` | `string` | optional | — | When the specified storage bucket is a 1st gen function uploard url bucket, this field should be set as the generated upload url for 1st gen deployment. |
| `buildConfig.sourceProvenance` | `source_provenance` | `map` | output only | — | Output only. A permanent fixed identifier for source. |
| `buildConfig.sourceProvenance.gitUri` | `git_uri` | `string` | optional | — | A copy of the build's `source.git_uri`, if exists, with any commits resolved. |
| `buildConfig.sourceProvenance.resolvedRepoSource` | `resolved_repo_source` | `map` | optional | — | A copy of the build's `source.repo_source`, if exists, with any revisions resolved. |
| `buildConfig.sourceProvenance.resolvedRepoSource.branchName` | `branch_name` | `string` | optional | — | Regex matching branches to build. The syntax of the regular expressions accepted is the syntax accepted by RE2 and described at https://github.com/google/re2/wiki/Syntax |
| `buildConfig.sourceProvenance.resolvedRepoSource.commitSha` | `commit_sha` | `string` | optional | — | Explicit commit SHA to build. |
| `buildConfig.sourceProvenance.resolvedRepoSource.dir` | — | `string` | optional | — | Directory, relative to the source root, in which to run the build. This must be a relative path. If a step's `dir` is specified and is an absolute path, this value is ignored for that step's … |
| `buildConfig.sourceProvenance.resolvedRepoSource.projectId` | `project_id` | `string` | optional | — | ID of the project that owns the Cloud Source Repository. If omitted, the project ID requesting the build is assumed. |
| `buildConfig.sourceProvenance.resolvedRepoSource.repoName` | `repo_name` | `string` | optional | — | Name of the Cloud Source Repository. |
| `buildConfig.sourceProvenance.resolvedRepoSource.tagName` | `tag_name` | `string` | optional | — | Regex matching tags to build. The syntax of the regular expressions accepted is the syntax accepted by RE2 and described at https://github.com/google/re2/wiki/Syntax |
| `buildConfig.sourceProvenance.resolvedStorageSource` | `resolved_storage_source` | `map` | optional | — | A copy of the build's `source.storage_source`, if exists, with any generations resolved. |
| `buildConfig.sourceProvenance.resolvedStorageSource.bucket` | — | `string` | optional | — | Google Cloud Storage bucket containing the source (see \[Bucket Name Requirements\](https://cloud.google.com/storage/docs/bucket-naming#requirements)). |
| `buildConfig.sourceProvenance.resolvedStorageSource.generation` | — | `string` | optional | — | Google Cloud Storage generation for the object. If the generation is omitted, the latest generation will be used. |
| `buildConfig.sourceProvenance.resolvedStorageSource.object` | — | `string` | optional | — | Google Cloud Storage object containing the source. This object must be a gzipped archive file (`.tar.gz`) containing source to build. |
| `buildConfig.sourceProvenance.resolvedStorageSource.sourceUploadUrl` | `source_upload_url` | `string` | optional | — | When the specified storage bucket is a 1st gen function uploard url bucket, this field should be set as the generated upload url for 1st gen deployment. |
| `buildConfig.sourceToken` | `source_token` | `string` | optional | — | An identifier for Firebase function sources. Disclaimer: This field is only supported for Firebase function deployments. |
| `buildConfig.workerPool` | `worker_pool` | `string` | optional | — | Name of the Cloud Build Custom Worker Pool that should be used to build the function. The format of this field is `projects/{project}/locations/{region}/workerPools/{workerPool}` where {project} and … |
| `createTime` | `create_time` | `string` | output only | — | Output only. The create timestamp of a Cloud Function. This is only applicable to 2nd Gen functions. |
| `description` | — | `string` | optional | — | User-provided description of a function. |
| `environment` | — | `string` | optional | — | Describe whether the function is 1st Gen or 2nd Gen. |
| `eventTrigger` | `event_trigger` | `map` | optional | — | An Eventarc trigger managed by Google Cloud Functions that fires events in response to a condition in another service. |
| `eventTrigger.channel` | — | `string` | optional | — | Optional. The name of the channel associated with the trigger in `projects/{project}/locations/{location}/channels/{channel}` format. You must provide a channel to receive events from Eventarc SaaS … |
| `eventTrigger.eventFilters` | `event_filters` | `list` | optional | — | Criteria used to filter events. |
| `eventTrigger.eventFilters[]` | — | `map` | optional | — | — |
| `eventTrigger.eventFilters[].attribute` | — | `string` | optional | — | Required. The name of a CloudEvents attribute. |
| `eventTrigger.eventFilters[].operator` | — | `string` | optional | — | Optional. The operator used for matching the events with the value of the filter. If not specified, only events that have an exact key-value pair specified in the filter are matched. The only allowed … |
| `eventTrigger.eventFilters[].value` | — | `string` | optional | — | Required. The value for the attribute. |
| `eventTrigger.eventType` | `event_type` | `string` | optional | — | Required. The type of event to observe. For example: `google.cloud.audit.log.v1.written` or `google.cloud.pubsub.topic.v1.messagePublished`. |
| `eventTrigger.pubsubTopic` | `pubsub_topic` | `string` | optional | — | Optional. The name of a Pub/Sub topic in the same project that will be used as the transport topic for the event delivery. Format: `projects/{project}/topics/{topic}`. This is only valid for events … |
| `eventTrigger.retryPolicy` | `retry_policy` | `string` | optional | — | Optional. If unset, then defaults to ignoring failures (i.e. not retrying them). |
| `eventTrigger.service` | — | `string` | optional | — | Optional. The hostname of the service that 1st Gen function should be observed. If no string is provided, the default service implementing the API will be used. For example, `storage.googleapis.com` … |
| `eventTrigger.serviceAccountEmail` | `service_account_email` | `string` | optional | — | Optional. The email of the trigger's service account. The service account must have permission to invoke Cloud Run services, the permission is `run.routes.invoke`. If empty, defaults to the Compute … |
| `eventTrigger.trigger` | — | `string` | output only | — | Output only. The resource name of the Eventarc trigger. The format of this field is `projects/{project}/locations/{region}/triggers/{trigger}`. |
| `eventTrigger.triggerRegion` | `trigger_region` | `string` | optional | — | The region that the trigger will be in. The trigger will only receive events originating in this region. It can be the same region as the function, a different region or multi-region, or the global … |
| `functionId` | — | `string` | optional | force new, create only | The ID to use for the function, which will become the final component of the function's resource name. This value should be 4-63 characters, and valid characters are /a-z-/. |
| `kmsKeyName` | `kms_key_name` | `string` | optional | — | Resource name of a KMS crypto key (managed by the user) used to encrypt/decrypt function resources. It must match the pattern … |
| `labels` | — | `map` | optional | opaque | Labels associated with this Cloud Function. |
| `name` | — | `string` | output only | — | A user-defined name of the function. Function names must be unique globally and match pattern `projects/*/locations/*/functions/*` |
| `satisfiesPzi` | `satisfies_pzi` | `boolean` | output only | — | Output only. Reserved for future use. |
| `satisfiesPzs` | `satisfies_pzs` | `boolean` | output only | — | Output only. Reserved for future use. |
| `serviceConfig` | `service_config` | `map` | optional | — | Describes the Service being deployed. Currently deploys services to Cloud Run (fully managed). |
| `serviceConfig.allTrafficOnLatestRevision` | `all_traffic_on_latest_revision` | `boolean` | optional | — | Whether 100% of traffic is routed to the latest revision. On CreateFunction and UpdateFunction, when set to true, the revision being deployed will serve 100% of traffic, ignoring any traffic split … |
| `serviceConfig.availableCpu` | `available_cpu` | `string` | optional | — | The number of CPUs used in a single container instance. Default value is calculated from available memory. Supports the same values as Cloud Run, see … |
| `serviceConfig.availableMemory` | `available_memory` | `string` | optional | — | The amount of memory available for a function. Defaults to 256M. Supported units are k, M, G, Mi, Gi. If no unit is supplied the value is interpreted as bytes. See … |
| `serviceConfig.binaryAuthorizationPolicy` | `binary_authorization_policy` | `string` | optional | — | Optional. The binary authorization policy to be checked when deploying the Cloud Run service. |
| `serviceConfig.directVpcEgress` | `direct_vpc_egress` | `string` | optional | — | Optional. Egress settings for direct VPC. If not provided, it defaults to VPC_EGRESS_PRIVATE_RANGES_ONLY. |
| `serviceConfig.directVpcNetworkInterface` | `direct_vpc_network_interface` | `list` | optional | — | Optional. The Direct VPC network interface for the Cloud Function. Currently only a single Direct VPC is supported. |
| `serviceConfig.directVpcNetworkInterface[]` | — | `map` | optional | — | — |
| `serviceConfig.directVpcNetworkInterface[].network` | — | `string` | optional | — | Optional. The name of the VPC network to which the function will be connected. Specify either a VPC network or a subnet, or both. If you specify only a network, the subnet uses the same name as the … |
| `serviceConfig.directVpcNetworkInterface[].subnetwork` | — | `string` | optional | — | Optional. The name of the VPC subnetwork that the Cloud Function resource will get IPs from. Specify either a VPC network or a subnet, or both. If both network and subnetwork are specified, the given … |
| `serviceConfig.directVpcNetworkInterface[].tags` | — | `list` | optional | — | Optional. Network tags applied to this Cloud Function resource. |
| `serviceConfig.directVpcNetworkInterface[].tags[]` | — | `string` | optional | — | — |
| `serviceConfig.environmentVariables` | `environment_variables` | `map` | optional | opaque | Environment variables that shall be available during function execution. |
| `serviceConfig.ingressSettings` | `ingress_settings` | `string` | optional | — | The ingress settings for the function, controlling what traffic can reach it. |
| `serviceConfig.maxInstanceCount` | `max_instance_count` | `integer` | optional | — | The limit on the maximum number of function instances that may coexist at a given time. In some cases, such as rapid traffic surges, Cloud Functions may, for a short period of time, create more … |
| `serviceConfig.maxInstanceRequestConcurrency` | `max_instance_request_concurrency` | `integer` | optional | — | Sets the maximum number of concurrent requests that each instance can receive. Defaults to 1. |
| `serviceConfig.minInstanceCount` | `min_instance_count` | `integer` | optional | — | The limit on the minimum number of function instances that may coexist at a given time. Function instances are kept in idle state for a short period after they finished executing the request to … |
| `serviceConfig.revision` | — | `string` | output only | — | Output only. The name of service revision. |
| `serviceConfig.secretEnvironmentVariables` | `secret_environment_variables` | `list` | optional | — | Secret environment variables configuration. |
| `serviceConfig.secretEnvironmentVariables[]` | — | `map` | optional | — | — |
| `serviceConfig.secretEnvironmentVariables[].key` | — | `string` | optional | — | Name of the environment variable. |
| `serviceConfig.secretEnvironmentVariables[].projectId` | `project_id` | `string` | optional | — | Project identifier (preferably project number but can also be the project ID) of the project that contains the secret. If not set, it is assumed that the secret is in the same project as the function. |
| `serviceConfig.secretEnvironmentVariables[].secret` | — | `string` | optional | — | Name of the secret in secret manager (not the full resource name). |
| `serviceConfig.secretEnvironmentVariables[].version` | — | `string` | optional | — | Version of the secret (version number or the string 'latest'). It is recommended to use a numeric version for secret environment variables as any updates to the secret value is not reflected until … |
| `serviceConfig.secretVolumes` | `secret_volumes` | `list` | optional | — | Secret volumes configuration. |
| `serviceConfig.secretVolumes[]` | — | `map` | optional | — | — |
| `serviceConfig.secretVolumes[].mountPath` | `mount_path` | `string` | optional | — | The path within the container to mount the secret volume. For example, setting the mount_path as `/etc/secrets` would mount the secret value files under the `/etc/secrets` directory. This directory … |
| `serviceConfig.secretVolumes[].projectId` | `project_id` | `string` | optional | — | Project identifier (preferably project number but can also be the project ID) of the project that contains the secret. If not set, it is assumed that the secret is in the same project as the function. |
| `serviceConfig.secretVolumes[].secret` | — | `string` | optional | — | Name of the secret in secret manager (not the full resource name). |
| `serviceConfig.secretVolumes[].versions` | — | `list` | optional | — | List of secret versions to mount for this secret. If empty, the `latest` version of the secret will be made available in a file named after the secret under the mount point. |
| `serviceConfig.secretVolumes[].versions[]` | — | `map` | optional | — | — |
| `serviceConfig.secretVolumes[].versions[].path` | — | `string` | optional | — | Relative path of the file under the mount path where the secret value for this version will be fetched and made available. For example, setting the mount_path as '/etc/secrets' and path as … |
| `serviceConfig.secretVolumes[].versions[].version` | — | `string` | optional | — | Version of the secret (version number or the string 'latest'). It is preferable to use `latest` version with secret volumes as secret value changes are reflected immediately. |
| `serviceConfig.securityLevel` | `security_level` | `string` | optional | — | Security level configure whether the function only accepts https. This configuration is only applicable to 1st Gen functions with Http trigger. By default https is optional for 1st Gen functions; 2nd … |
| `serviceConfig.service` | — | `string` | output only | — | Output only. Name of the service associated with a Function. The format of this field is `projects/{project}/locations/{region}/services/{service}` |
| `serviceConfig.serviceAccountEmail` | `service_account_email` | `string` | optional | — | The email of the service's service account. If empty, defaults to `{project_number}-compute@developer.gserviceaccount.com`. |
| `serviceConfig.timeoutSeconds` | `timeout_seconds` | `integer` | optional | — | The function execution timeout. Execution is considered failed and can be terminated if the function is not completed at the end of the timeout period. Defaults to 60 seconds. |
| `serviceConfig.uri` | — | `string` | output only | — | Output only. URI of the Service deployed. |
| `serviceConfig.vpcConnector` | `vpc_connector` | `string` | optional | — | The Serverless VPC Access connector that this cloud function can connect to. The format of this field is `projects/*/locations/*/connectors/*`. |
| `serviceConfig.vpcConnectorEgressSettings` | `vpc_connector_egress_settings` | `string` | optional | — | The egress settings for the connector, controlling what traffic is diverted through it. |
| `state` | — | `string` | output only | — | Output only. State of the function. |
| `stateMessages` | `state_messages` | `list` | output only | — | Output only. State Messages for this Cloud Function. |
| `stateMessages[]` | — | `map` | optional | — | — |
| `stateMessages[].message` | — | `string` | optional | — | The message. |
| `stateMessages[].severity` | — | `string` | optional | — | Severity of the state message. |
| `stateMessages[].type` | — | `string` | optional | — | One-word CamelCase type of the state message. |
| `updateTime` | `update_time` | `string` | output only | — | Output only. The last update timestamp of a Cloud Function. |
| `upgradeInfo` | `upgrade_info` | `map` | output only | — | Output only. UpgradeInfo for this Cloud Function |
| `upgradeInfo.buildConfig` | `build_config` | `map` | optional | — | Describes the Build step of the function that builds a container to prepare for 2nd gen upgrade. |
| `upgradeInfo.buildConfig.automaticUpdatePolicy` | `automatic_update_policy` | `map` | optional | opaque | Security patches are applied automatically to the runtime without requiring the function to be redeployed. |
| `upgradeInfo.buildConfig.build` | — | `string` | output only | — | Output only. The Cloud Build name of the latest successful deployment of the function. |
| `upgradeInfo.buildConfig.dockerRegistry` | `docker_registry` | `string` | optional | — | Docker Registry to use for this deployment. This configuration is only applicable to 1st Gen functions, 2nd Gen functions can only use Artifact Registry. Deprecated: as of March 2025 … |
| `upgradeInfo.buildConfig.dockerRepository` | `docker_repository` | `string` | optional | — | Repository in Artifact Registry to which the function docker image will be pushed after it is built by Cloud Build. If specified by user, it is created and managed by user with a customer managed … |
| `upgradeInfo.buildConfig.entryPoint` | `entry_point` | `string` | optional | — | The name of the function (as defined in source code) that will be executed. Defaults to the resource name suffix, if not specified. For backward compatibility, if function with given name is not … |
| `upgradeInfo.buildConfig.environmentVariables` | `environment_variables` | `map` | optional | opaque | User-provided build-time environment variables for the function |
| `upgradeInfo.buildConfig.onDeployUpdatePolicy` | `on_deploy_update_policy` | `map` | optional | same fields as `buildConfig.onDeployUpdatePolicy` | Security patches are only applied when a function is redeployed. |
| `upgradeInfo.buildConfig.runtime` | — | `string` | optional | — | The runtime in which to run the function. Required when deploying a new function, optional when updating an existing function. For a complete list of possible choices, see the \[`gcloud` command … |
| `upgradeInfo.buildConfig.serviceAccount` | `service_account` | `string` | optional | — | Service account to be used for building the container. The format of this field is `projects/{projectId}/serviceAccounts/{serviceAccountEmail}`. |
| `upgradeInfo.buildConfig.source` | — | `map` | optional | same fields as `buildConfig.source` | The location of the function source code. |
| `upgradeInfo.buildConfig.sourceProvenance` | `source_provenance` | `map` | output only | same fields as `buildConfig.sourceProvenance` | Output only. A permanent fixed identifier for source. |
| `upgradeInfo.buildConfig.sourceToken` | `source_token` | `string` | optional | — | An identifier for Firebase function sources. Disclaimer: This field is only supported for Firebase function deployments. |
| `upgradeInfo.buildConfig.workerPool` | `worker_pool` | `string` | optional | — | Name of the Cloud Build Custom Worker Pool that should be used to build the function. The format of this field is `projects/{project}/locations/{region}/workerPools/{workerPool}` where {project} and … |
| `upgradeInfo.eventTrigger` | `event_trigger` | `map` | optional | — | Describes the Event trigger which has been setup to prepare for 2nd gen upgrade. |
| `upgradeInfo.eventTrigger.channel` | — | `string` | optional | — | Optional. The name of the channel associated with the trigger in `projects/{project}/locations/{location}/channels/{channel}` format. You must provide a channel to receive events from Eventarc SaaS … |
| `upgradeInfo.eventTrigger.eventFilters` | `event_filters` | `list` | optional | same fields as `eventTrigger.eventFilters` | Criteria used to filter events. |
| `upgradeInfo.eventTrigger.eventType` | `event_type` | `string` | optional | — | Required. The type of event to observe. For example: `google.cloud.audit.log.v1.written` or `google.cloud.pubsub.topic.v1.messagePublished`. |
| `upgradeInfo.eventTrigger.pubsubTopic` | `pubsub_topic` | `string` | optional | — | Optional. The name of a Pub/Sub topic in the same project that will be used as the transport topic for the event delivery. Format: `projects/{project}/topics/{topic}`. This is only valid for events … |
| `upgradeInfo.eventTrigger.retryPolicy` | `retry_policy` | `string` | optional | — | Optional. If unset, then defaults to ignoring failures (i.e. not retrying them). |
| `upgradeInfo.eventTrigger.service` | — | `string` | optional | — | Optional. The hostname of the service that 1st Gen function should be observed. If no string is provided, the default service implementing the API will be used. For example, `storage.googleapis.com` … |
| `upgradeInfo.eventTrigger.serviceAccountEmail` | `service_account_email` | `string` | optional | — | Optional. The email of the trigger's service account. The service account must have permission to invoke Cloud Run services, the permission is `run.routes.invoke`. If empty, defaults to the Compute … |
| `upgradeInfo.eventTrigger.trigger` | — | `string` | output only | — | Output only. The resource name of the Eventarc trigger. The format of this field is `projects/{project}/locations/{region}/triggers/{trigger}`. |
| `upgradeInfo.eventTrigger.triggerRegion` | `trigger_region` | `string` | optional | — | The region that the trigger will be in. The trigger will only receive events originating in this region. It can be the same region as the function, a different region or multi-region, or the global … |
| `upgradeInfo.serviceConfig` | `service_config` | `map` | optional | — | Describes the Cloud Run service which has been setup to prepare for 2nd gen upgrade. |
| `upgradeInfo.serviceConfig.allTrafficOnLatestRevision` | `all_traffic_on_latest_revision` | `boolean` | optional | — | Whether 100% of traffic is routed to the latest revision. On CreateFunction and UpdateFunction, when set to true, the revision being deployed will serve 100% of traffic, ignoring any traffic split … |
| `upgradeInfo.serviceConfig.availableCpu` | `available_cpu` | `string` | optional | — | The number of CPUs used in a single container instance. Default value is calculated from available memory. Supports the same values as Cloud Run, see … |
| `upgradeInfo.serviceConfig.availableMemory` | `available_memory` | `string` | optional | — | The amount of memory available for a function. Defaults to 256M. Supported units are k, M, G, Mi, Gi. If no unit is supplied the value is interpreted as bytes. See … |
| `upgradeInfo.serviceConfig.binaryAuthorizationPolicy` | `binary_authorization_policy` | `string` | optional | — | Optional. The binary authorization policy to be checked when deploying the Cloud Run service. |
| `upgradeInfo.serviceConfig.directVpcEgress` | `direct_vpc_egress` | `string` | optional | — | Optional. Egress settings for direct VPC. If not provided, it defaults to VPC_EGRESS_PRIVATE_RANGES_ONLY. |
| `upgradeInfo.serviceConfig.directVpcNetworkInterface` | `direct_vpc_network_interface` | `list` | optional | same fields as `serviceConfig.directVpcNetworkInterface` | Optional. The Direct VPC network interface for the Cloud Function. Currently only a single Direct VPC is supported. |
| `upgradeInfo.serviceConfig.environmentVariables` | `environment_variables` | `map` | optional | opaque | Environment variables that shall be available during function execution. |
| `upgradeInfo.serviceConfig.ingressSettings` | `ingress_settings` | `string` | optional | — | The ingress settings for the function, controlling what traffic can reach it. |
| `upgradeInfo.serviceConfig.maxInstanceCount` | `max_instance_count` | `integer` | optional | — | The limit on the maximum number of function instances that may coexist at a given time. In some cases, such as rapid traffic surges, Cloud Functions may, for a short period of time, create more … |
| `upgradeInfo.serviceConfig.maxInstanceRequestConcurrency` | `max_instance_request_concurrency` | `integer` | optional | — | Sets the maximum number of concurrent requests that each instance can receive. Defaults to 1. |
| `upgradeInfo.serviceConfig.minInstanceCount` | `min_instance_count` | `integer` | optional | — | The limit on the minimum number of function instances that may coexist at a given time. Function instances are kept in idle state for a short period after they finished executing the request to … |
| `upgradeInfo.serviceConfig.revision` | — | `string` | output only | — | Output only. The name of service revision. |
| `upgradeInfo.serviceConfig.secretEnvironmentVariables` | `secret_environment_variables` | `list` | optional | same fields as `serviceConfig.secretEnvironmentVariables` | Secret environment variables configuration. |
| `upgradeInfo.serviceConfig.secretVolumes` | `secret_volumes` | `list` | optional | same fields as `serviceConfig.secretVolumes` | Secret volumes configuration. |
| `upgradeInfo.serviceConfig.securityLevel` | `security_level` | `string` | optional | — | Security level configure whether the function only accepts https. This configuration is only applicable to 1st Gen functions with Http trigger. By default https is optional for 1st Gen functions; 2nd … |
| `upgradeInfo.serviceConfig.service` | — | `string` | output only | — | Output only. Name of the service associated with a Function. The format of this field is `projects/{project}/locations/{region}/services/{service}` |
| `upgradeInfo.serviceConfig.serviceAccountEmail` | `service_account_email` | `string` | optional | — | The email of the service's service account. If empty, defaults to `{project_number}-compute@developer.gserviceaccount.com`. |
| `upgradeInfo.serviceConfig.timeoutSeconds` | `timeout_seconds` | `integer` | optional | — | The function execution timeout. Execution is considered failed and can be terminated if the function is not completed at the end of the timeout period. Defaults to 60 seconds. |
| `upgradeInfo.serviceConfig.uri` | — | `string` | output only | — | Output only. URI of the Service deployed. |
| `upgradeInfo.serviceConfig.vpcConnector` | `vpc_connector` | `string` | optional | — | The Serverless VPC Access connector that this cloud function can connect to. The format of this field is `projects/*/locations/*/connectors/*`. |
| `upgradeInfo.serviceConfig.vpcConnectorEgressSettings` | `vpc_connector_egress_settings` | `string` | optional | — | The egress settings for the connector, controlling what traffic is diverted through it. |
| `upgradeInfo.upgradeState` | `upgrade_state` | `string` | optional | — | UpgradeState of the function |
| `url` | — | `string` | output only | — | Output only. The deployed url for the function. |
