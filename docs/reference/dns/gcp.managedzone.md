<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.managedzone`

A zone is a subtree of the DNS namespace under one administrative responsibility. A ManagedZone is a resource that represents a DNS zone hosted by the Cloud DNS service.

[dns](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | dns |
| Scope | global |
| Asset type | `dns.googleapis.com/ManagedZone` |
| Tier | 1 (ruled: description is required on evidence: the live probe on 2026-09-24 created a private zone without one and Google answered 400, "The 'entity.managedZone.description' parameter is required but was missing". Terraform always sends "Managed by Terraform".
Both hooks are Terraform's, and one of them exists only because magic-modules picked a different update method than this provider does.
update_encoder (update_encoder/managed_dns_zone.go.tmpl) re-adds `nameServers`, `id` and `creationTime` -- all output-only fields -- to the update body, and its own comment says why: "The upstream update method requires the full ManagedZones object". That is dns.managedZones.UPDATE, a PUT full replace. This provider uses dns.managedZones.PATCH, which the Discovery document publishes at the same path with a partial ManagedZone body (both methods checked there, 2026-09-23). Under PATCH, naming an output-only field in the body is at best ignored and at worst rejected, so the hook is not merely unnecessary here, it would be wrong to apply.
pre_delete (pre_delete/managed_dns_zone.go.tmpl) is gated on `force_destroy`, a Terraform-only convenience field with no counterpart in the DNS API. When set it pages through every record set in the zone and deletes each one -- skipping NS and SOA, which the API refuses -- before deleting the zone. We do not offer the field, so the hook has nothing to fire on, the same shape as compute/Network's post_create above.
THE BOUNDED GAP, stated rather than discovered: deleting a zone that still holds records fails, with Google's own error. That is the API's own rule and it is loud, not silent; the fix is to remove the records first.
THE ONE THING THAT LOOKED UNKNOWN, AND WAS NOT. dns.managedZones CREATE returns a ManagedZone, so the type's await strategy is None -- but its PATCH returns an Operation, and await is a property of the TYPE, not of the method. Nothing can be corrupted by that, because Update discards what await returns and reads its own state back. The question was only whether that read could come before the patch had landed.
Google's own description answers it. The dns v1 Operation is "a successful mutation performed on a Cloud DNS resource" -- an audit record of a change already made -- and it carries zoneContext.newValue, "the post-operation ManagedZone resource". Its PENDING/DONE status is about propagation to the authoritative name servers ("the request to update the authoritative servers has been sent, but the servers might not be updated yet"), not about the zone resource. So the read-back sees the patched zone, and name-server propagation is not something a plan should wait on. Settled from the documentation on 2026-09-23; not yet observed against a real zone.
Unblocks gcp.resourcerecordset, which ships today and has nowhere to put a record.
) |
| Mutation timeout | 60s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/managedZones` |
| Read | yes | `GET projects/{project}/managedZones/{managedZone}` |
| Update | yes | `PATCH projects/{project}/managedZones/{managedZone}` |
| Delete | yes | `DELETE projects/{project}/managedZones/{managedZone}` |
| Import | yes | by id, see below |

## Import id

```
projects/{project}/managedZones/{managedZone}
```

## Attributes

57 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `cloudLoggingConfig` | `cloud_logging_config` | `map` | optional | — | Cloud Logging configurations for publicly visible zones. |
| `cloudLoggingConfig.enableLogging` | `enable_logging` | `boolean` | required | — | If set, enable query logging for this ManagedZone. False by default, making logging opt-in. |
| `cloudLoggingConfig.kind` | — | `string` | output only | — | — |
| `creationTime` | `creation_time` | `string` | output only | — | The time that this resource was created on the server. This is in RFC3339 text format. Output only. |
| `description` | — | `string` | required | — | A mutable string of at most 1024 characters associated with this resource for the user's convenience. Has no effect on the managed zone's function. |
| `dnsName` | `dns_name` | `string` | required | force new | The DNS name of this managed zone, for instance "example.com.". |
| `dnssecConfig` | `dnssec_config` | `map` | optional | — | DNSSEC configuration. |
| `dnssecConfig.defaultKeySpecs` | `default_key_specs` | `list` | optional | — | Specifies parameters for generating initial DnsKeys for this ManagedZone. Can only be changed while the state is OFF. |
| `dnssecConfig.defaultKeySpecs[]` | — | `map` | optional | — | — |
| `dnssecConfig.defaultKeySpecs[].algorithm` | — | `string` | optional | — | String mnemonic specifying the DNSSEC algorithm of this key. |
| `dnssecConfig.defaultKeySpecs[].keyLength` | `key_length` | `integer` | optional | — | Length of the keys in bits. |
| `dnssecConfig.defaultKeySpecs[].keyType` | `key_type` | `string` | optional | — | Specifies whether this is a key signing key (KSK) or a zone signing key (ZSK). Key signing keys have the Secure Entry Point flag set and, when active, are only used to sign resource record sets of … |
| `dnssecConfig.defaultKeySpecs[].kind` | — | `string` | output only | — | — |
| `dnssecConfig.kind` | — | `string` | output only | — | — |
| `dnssecConfig.nonExistence` | `non_existence` | `string` | optional | — | Specifies the mechanism for authenticated denial-of-existence responses. Can only be changed while the state is OFF. |
| `dnssecConfig.state` | — | `string` | optional | — | Specifies whether DNSSEC is enabled, and what mode it is in. |
| `forwardingConfig` | `forwarding_config` | `map` | optional | — | The presence for this field indicates that outbound forwarding is enabled for this zone. The value of this field contains the set of destinations to forward to. |
| `forwardingConfig.kind` | — | `string` | output only | — | — |
| `forwardingConfig.targetNameServers` | `target_name_servers` | `list` | required | unordered | List of target name servers to forward to. Cloud DNS selects the best available name server if more than one target is given. |
| `forwardingConfig.targetNameServers[]` | — | `map` | optional | — | — |
| `forwardingConfig.targetNameServers[].domainName` | `domain_name` | `string` | optional | — | Fully qualified domain name for the forwarding target. |
| `forwardingConfig.targetNameServers[].forwardingPath` | `forwarding_path` | `string` | optional | — | Forwarding path for this NameServerTarget. If unset or set to DEFAULT, Cloud DNS makes forwarding decisions based on IP address ranges; that is, RFC1918 addresses go to the VPC network, non-RFC1918 … |
| `forwardingConfig.targetNameServers[].ipv4Address` | `ipv4_address` | `string` | optional | — | IPv4 address of a target name server. |
| `forwardingConfig.targetNameServers[].ipv6Address` | `ipv6_address` | `string` | optional | — | IPv6 address of a target name server. Does not accept both fields (ipv4 & ipv6) being populated. Public preview as of November 2022. |
| `forwardingConfig.targetNameServers[].kind` | — | `string` | output only | — | — |
| `id` | — | `string` | output only | — | Unique identifier for the resource; defined by the server (output only) |
| `kind` | — | `string` | output only | — | — |
| `labels` | — | `map` | optional | opaque | User labels. |
| `name` | — | `string` | required | force new | User assigned name for this resource. Must be unique within the project. The name must be 1-63 characters long, must begin with a letter, end with a letter or digit, and only contain lowercase … |
| `nameServerSet` | `name_server_set` | `string` | optional | — | Optionally specifies the NameServerSet for this ManagedZone. A NameServerSet is a set of DNS name servers that all host the same ManagedZones. Most users leave this field unset. If you need to use … |
| `nameServers` | `name_servers` | `list` | output only | — | Delegate your managed_zone to these virtual name servers; defined by the server (output only) |
| `nameServers[]` | — | `string` | optional | — | — |
| `peeringConfig` | `peering_config` | `map` | optional | — | The presence of this field indicates that DNS Peering is enabled for this zone. The value of this field contains the network to peer with. |
| `peeringConfig.kind` | — | `string` | output only | — | — |
| `peeringConfig.targetNetwork` | `target_network` | `map` | required | — | The network with which to peer. |
| `peeringConfig.targetNetwork.deactivateTime` | `deactivate_time` | `string` | optional | — | The time at which the zone was deactivated, in RFC 3339 date-time format. An empty string indicates that the peering connection is active. The producer network can deactivate a zone. The zone is … |
| `peeringConfig.targetNetwork.kind` | — | `string` | output only | — | — |
| `peeringConfig.targetNetwork.networkUrl` | `network_url` | `string` | required | — | The fully qualified URL of the VPC network to forward queries to. This should be formatted like `https://www.googleapis.com/compute/v1/projects/{project}/global/networks/{network}` |
| `privateVisibilityConfig` | `private_visibility_config` | `map` | optional | — | For privately visible zones, the set of Virtual Private Cloud resources that the zone is visible from. |
| `privateVisibilityConfig.gkeClusters` | `gke_clusters` | `list` | optional | — | The list of Google Kubernetes Engine clusters that can see this zone. |
| `privateVisibilityConfig.gkeClusters[]` | — | `map` | optional | — | — |
| `privateVisibilityConfig.gkeClusters[].gkeClusterName` | `gke_cluster_name` | `string` | required | — | The resource name of the cluster to bind this ManagedZone to. This should be specified in the format like: projects/*/locations/*/clusters/*. This is referenced from GKE … |
| `privateVisibilityConfig.gkeClusters[].kind` | — | `string` | output only | — | — |
| `privateVisibilityConfig.kind` | — | `string` | output only | — | — |
| `privateVisibilityConfig.networks` | — | `list` | optional | unordered | The list of VPC networks that can see this zone. |
| `privateVisibilityConfig.networks[]` | — | `map` | optional | — | — |
| `privateVisibilityConfig.networks[].kind` | — | `string` | output only | — | — |
| `privateVisibilityConfig.networks[].networkUrl` | `network_url` | `string` | required | — | The fully qualified URL of the VPC network to bind to. Format this URL like `https://www.googleapis.com/compute/v1/projects/{project}/global/networks/{network}` |
| `reverseLookupConfig` | `reverse_lookup_config` | `map` | optional | force new | The presence of this field indicates that this is a managed reverse lookup zone and Cloud DNS resolves reverse lookup queries using automatically configured records for VPC resources. This only … |
| `reverseLookupConfig.kind` | — | `string` | output only | — | — |
| `serviceDirectoryConfig` | `service_directory_config` | `map` | optional | force new | This field links to the associated service directory namespace. Do not set this field for public zones or forwarding zones. |
| `serviceDirectoryConfig.kind` | — | `string` | output only | — | — |
| `serviceDirectoryConfig.namespace` | — | `map` | required | — | Contains information about the namespace associated with the zone. |
| `serviceDirectoryConfig.namespace.deletionTime` | `deletion_time` | `string` | optional | — | The time that the namespace backing this zone was deleted; an empty string if it still exists. This is in RFC3339 text format. Output only. |
| `serviceDirectoryConfig.namespace.kind` | — | `string` | output only | — | — |
| `serviceDirectoryConfig.namespace.namespaceUrl` | `namespace_url` | `string` | required | — | The fully qualified URL of the namespace associated with the zone. Format must be `https://servicedirectory.googleapis.com/v1/projects/{project}/locations/{location}/namespaces/{namespace}` |
| `visibility` | — | `string` | optional | force new | The zone's visibility: public zones are exposed to the Internet, while private zones are visible only to Virtual Private Cloud resources. |
