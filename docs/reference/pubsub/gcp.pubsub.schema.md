<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.pubsub.schema`

A schema is a format that messages must follow, creating a contract between publisher and subscriber that Pub/Sub will enforce.

[pubsub](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | pubsub |
| Scope | global |
| Asset type | `pubsub.googleapis.com/Schema` |
| Hangs off | `projects` |
| Tier | 1 (ruled: update_encoder (update_encoder/pubsub_schema.tmpl) wraps the body for a schema commit, a new revision: magic-modules declares update_verb POST to schemas/{name}:commit. This provider does not commit revisions, and the generator refuses any update verb but PATCH, so any change replaces the schema. (An earlier version of this note said "a schema has no patch" and missed that the declared POST reached the catalog anyway, until the generator stopped letting it.)
) |
| Mutation timeout | 60s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/schemas?schemaId={{name}}` |
| Read | yes | `GET {+name}` |
| Update | **no** | publishes no update method; every change to it replaces the resource |
| Delete | yes | `DELETE {+name}` |
| Import | yes | by id, see below |

## Import id

```
projects/{project}/schemas/{schema}
```

## Attributes

8 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `compiledProtoSchema` | `compiled_proto_schema` | `map` | optional | — | Optional. Configuration for a schema provided as a pre-compiled Protocol Buffer FileDescriptorSet. The `type` field above must be set to PROTOCOL_BUFFER. |
| `compiledProtoSchema.compiledBytes` | `compiled_bytes` | `string` | optional | — | Required. The compiled FileDescriptorSet binary. |
| `compiledProtoSchema.rootMessage` | `root_message` | `string` | optional | — | Required. The name of the root message type in the schema. |
| `definition` | — | `string` | optional | — | The definition of the schema. This should contain a string representing the full definition of the schema that is a valid schema definition of the type specified in `type`. |
| `name` | — | `string` | required | force new | Required. Name of the schema. Format is `projects/{project}/schemas/{schema}`. |
| `revisionCreateTime` | `revision_create_time` | `string` | output only | — | Output only. The timestamp that the revision was created. |
| `revisionId` | `revision_id` | `string` | output only | force new | Output only. Immutable. The revision ID of the schema. |
| `type_value` | wire `type` | `string` | optional | — | The type of the schema definition. |
