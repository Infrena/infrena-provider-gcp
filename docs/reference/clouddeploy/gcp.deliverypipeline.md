<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.deliverypipeline`

A DeliveryPipeline defines a pipeline through which a Skaffold configuration can progress.

[clouddeploy](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | clouddeploy |
| Scope | regional |
| Asset type | `clouddeploy.googleapis.com/DeliveryPipeline` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 1200s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/locations/{{location}}/deliveryPipelines` |
| Read | yes | `GET projects/{{project}}/locations/{{location}}/deliveryPipelines/{{name}}` |
| Update | **no** | publishes no update method; every change to it replaces the resource |
| Delete | yes | `DELETE projects/{{project}}/locations/{{location}}/deliveryPipelines/{{name}}` |
| Import | yes | by id, see below |

## Import id

```
projects/{{project}}/locations/{{location}}/deliveryPipelines/{{name}}
```

## Attributes

146 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `annotations` | — | `map` | optional | opaque | Optional. User annotations. These attributes can only be set and used by the user, and not by Cloud Deploy. |
| `condition` | — | `map` | output only | — | Output only. Information around the state of the Delivery Pipeline. |
| `condition.pipelineReadyCondition` | `pipeline_ready_condition` | `map` | optional | — | Details around the Pipeline's overall status. |
| `condition.pipelineReadyCondition.status` | — | `boolean` | optional | — | True if the Pipeline is in a valid state. Otherwise at least one condition in `PipelineCondition` is in an invalid state. Iterate over those conditions and see which condition(s) has status = false … |
| `condition.pipelineReadyCondition.updateTime` | `update_time` | `string` | optional | — | Last time the condition was updated. |
| `condition.targetsPresentCondition` | `targets_present_condition` | `map` | optional | — | Details around targets enumerated in the pipeline. |
| `condition.targetsPresentCondition.missingTargets` | `missing_targets` | `list` | optional | — | The list of Target names that do not exist. For example, `projects/{project_id}/locations/{location_name}/targets/{target_name}`. |
| `condition.targetsPresentCondition.missingTargets[]` | — | `string` | optional | — | — |
| `condition.targetsPresentCondition.status` | — | `boolean` | optional | — | True if there aren't any missing Targets. |
| `condition.targetsPresentCondition.updateTime` | `update_time` | `string` | optional | — | Last time the condition was updated. |
| `condition.targetsTypeCondition` | `targets_type_condition` | `map` | optional | — | Details on the whether the targets enumerated in the pipeline are of the same type. |
| `condition.targetsTypeCondition.errorDetails` | `error_details` | `string` | optional | — | Human readable error message. |
| `condition.targetsTypeCondition.status` | — | `boolean` | optional | — | True if the targets are all a comparable type. For example this is true if all targets are GKE clusters. This is false if some targets are Cloud Run targets and others are GKE clusters. |
| `createTime` | `create_time` | `string` | output only | — | Output only. Time at which the pipeline was created. |
| `description` | — | `string` | optional | — | Optional. Description of the `DeliveryPipeline`. Max length is 255 characters. |
| `etag` | — | `string` | optional | — | This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. |
| `labels` | — | `map` | optional | opaque | Optional. Labels are attributes that can be set and used by both the user and by Cloud Deploy. Labels must meet the following constraints: * Keys and values can contain only lowercase letters … |
| `name` | — | `string` | required | — | Identifier. Name of the `DeliveryPipeline`. Format is `projects/{project}/locations/{location}/deliveryPipelines/{deliveryPipeline}`. The `deliveryPipeline` component must match … |
| `serialPipeline` | `serial_pipeline` | `map` | optional | — | Optional. SerialPipeline defines a sequential set of stages for a `DeliveryPipeline`. |
| `serialPipeline.stages` | — | `list` | optional | — | Optional. Each stage specifies configuration for a `Target`. The ordering of this list defines the promotion flow. |
| `serialPipeline.stages[]` | — | `map` | optional | — | — |
| `serialPipeline.stages[].deployParameters` | `deploy_parameters` | `list` | optional | — | Optional. The deploy parameters to use for the target in this stage. |
| `serialPipeline.stages[].deployParameters[]` | — | `map` | optional | — | — |
| `serialPipeline.stages[].deployParameters[].matchTargetLabels` | `match_target_labels` | `map` | optional | opaque | Optional. Deploy parameters are applied to targets with match labels. If unspecified, deploy parameters are applied to all targets (including child targets of a multi-target). |
| `serialPipeline.stages[].deployParameters[].values` | — | `map` | optional | opaque | Required. Values are deploy parameters in key-value pairs. |
| `serialPipeline.stages[].profiles` | — | `list` | optional | — | Optional. Skaffold profiles to use when rendering the manifest for this stage's `Target`. |
| `serialPipeline.stages[].profiles[]` | — | `string` | optional | — | — |
| `serialPipeline.stages[].strategy` | — | `map` | optional | — | Optional. The strategy to use for a `Rollout` to this stage. |
| `serialPipeline.stages[].strategy.canary` | — | `map` | optional | — | Optional. Canary deployment strategy provides progressive percentage based deployments to a Target. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment` | `canary_deployment` | `map` | optional | — | Optional. Configures the progressive based deployment for a Target. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis` | — | `map` | optional | — | Optional. Configuration for the analysis job. If configured, the analysis will run after each percentage deployment. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.customChecks` | `custom_checks` | `list` | optional | — | Optional. Custom analysis checks from 3P metric providers. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.customChecks[]` | — | `map` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.customChecks[].frequency` | — | `string` | optional | — | Optional. The frequency at which the custom check will be run, with a minimum and default of 5 minutes. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.customChecks[].id` | — | `string` | optional | — | Required. The ID of the custom Analysis check. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.customChecks[].task` | — | `map` | optional | — | Required. The Task to be run for this custom check. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.customChecks[].task.container` | — | `map` | optional | opaque | Optional. This task is represented by a container that is executed in the Cloud Build execution environment. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.duration` | — | `string` | optional | — | Required. The amount of time in minutes the analysis on the target will last. If all analysis checks have successfully completed before the specified duration, the analysis is successful. If a check … |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.googleCloud` | `google_cloud` | `map` | optional | — | Optional. Google Cloud - based analysis checks. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.googleCloud.alertPolicyChecks` | `alert_policy_checks` | `list` | optional | — | Optional. A list of Cloud Monitoring Alert Policy checks to perform as part of the analysis. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.googleCloud.alertPolicyChecks[]` | — | `map` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.googleCloud.alertPolicyChecks[].alertPolicies` | `alert_policies` | `list` | optional | — | Required. The Cloud Monitoring Alert Policies to check for active alerts. Format is `projects/{project}/alertPolicies/{alert_policy}`. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.googleCloud.alertPolicyChecks[].alertPolicies[]` | — | `string` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.googleCloud.alertPolicyChecks[].id` | — | `string` | optional | — | Required. The ID of the analysis check. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.googleCloud.alertPolicyChecks[].labels` | — | `map` | optional | opaque | Optional. A set of labels to filter active alerts. If set, only alerts having all of the specified labels will be considered. Otherwise, all active alerts will be considered. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.percentages` | — | `list` | optional | — | Required. The percentage based deployments that will occur as a part of a `Rollout`. List is expected in ascending order and each integer n is 0 &lt;= n &lt; 100. If the GatewayServiceMesh is … |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.percentages[]` | — | `integer` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy` | — | `map` | optional | — | Optional. Configuration for the postdeploy job of the last phase. If this is not configured, there will be no postdeploy job for this phase. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.actions` | — | `list` | optional | — | Optional. A sequence of Skaffold custom actions to invoke during execution of the postdeploy job. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.actions[]` | — | `string` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.tasks` | — | `list` | optional | — | Optional. The tasks that will run as a part of the postdeploy job. The tasks are executed sequentially in the order specified. Only one of `actions` or `tasks` can be specified. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.tasks[]` | — | `map` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.tasks[].container` | — | `map` | optional | — | Optional. This task is represented by a container that is executed in the Cloud Build execution environment. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.tasks[].container.args` | — | `list` | optional | — | Optional. Args is the container arguments to use. This overrides the default arguments defined in the container image. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.tasks[].container.args[]` | — | `string` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.tasks[].container.command` | — | `list` | optional | — | Optional. Command is the container entrypoint to use. This overrides the default entrypoint defined in the container image. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.tasks[].container.command[]` | — | `string` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.tasks[].container.env` | — | `map` | optional | opaque | Optional. Environment variables that are set in the container. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.tasks[].container.image` | — | `string` | optional | — | Required. Image is the container image to use. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.predeploy` | — | `map` | optional | — | Optional. Configuration for the predeploy job of the first phase. If this is not configured, there will be no predeploy job for this phase. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.predeploy.actions` | — | `list` | optional | — | Optional. A sequence of Skaffold custom actions to invoke during execution of the predeploy job. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.predeploy.actions[]` | — | `string` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.predeploy.tasks` | — | `list` | optional | — | Optional. The tasks that will run as a part of the predeploy job. The tasks are executed sequentially in the order specified. Only one of `actions` or `tasks` can be specified. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.predeploy.tasks[]` | — | `map` | optional | same fields as `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.tasks[]` | — |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.verify` | — | `boolean` | optional | — | Optional. Whether to run verify tests after each percentage deployment via `skaffold verify`. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.verifyConfig` | `verify_config` | `map` | optional | — | Optional. Configuration for the verify job. Cannot be set if `verify` is set to true. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.verifyConfig.tasks` | — | `list` | optional | — | Optional. The tasks that will run as a part of the verify job. The tasks are executed sequentially in the order specified. |
| `serialPipeline.stages[].strategy.canary.canaryDeployment.verifyConfig.tasks[]` | — | `map` | optional | same fields as `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.tasks[]` | — |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment` | `custom_canary_deployment` | `map` | optional | — | Optional. Configures the progressive based deployment for a Target, but allows customizing at the phase level where a phase represents each of the percentage deployments. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs` | `phase_configs` | `list` | optional | — | Required. Configuration for each phase in the canary deployment in the order executed. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[]` | — | `map` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].analysis` | — | `map` | optional | — | Optional. Configuration for the analysis job of this phase. If this is not configured, there will be no analysis job for this phase. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].analysis.customChecks` | `custom_checks` | `list` | optional | — | Optional. Custom analysis checks from 3P metric providers. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].analysis.customChecks[]` | — | `map` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].analysis.customChecks[].frequency` | — | `string` | optional | — | Optional. The frequency at which the custom check will be run, with a minimum and default of 5 minutes. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].analysis.customChecks[].id` | — | `string` | optional | — | Required. The ID of the custom Analysis check. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].analysis.customChecks[].task` | — | `map` | optional | opaque | Required. The Task to be run for this custom check. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].analysis.duration` | — | `string` | optional | — | Required. The amount of time in minutes the analysis on the target will last. If all analysis checks have successfully completed before the specified duration, the analysis is successful. If a check … |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].analysis.googleCloud` | `google_cloud` | `map` | optional | — | Optional. Google Cloud - based analysis checks. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].analysis.googleCloud.alertPolicyChecks` | `alert_policy_checks` | `list` | optional | — | Optional. A list of Cloud Monitoring Alert Policy checks to perform as part of the analysis. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].analysis.googleCloud.alertPolicyChecks[]` | — | `map` | optional | opaque | — |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].percentage` | — | `integer` | optional | — | Required. Percentage deployment for the phase. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].phaseId` | `phase_id` | `string` | optional | — | Required. The ID to assign to the `Rollout` phase. This value must consist of lower-case letters, numbers, and hyphens, start with a letter and end with a letter or a number, and have a max length of … |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].postdeploy` | — | `map` | optional | — | Optional. Configuration for the postdeploy job of this phase. If this is not configured, there will be no postdeploy job for this phase. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].postdeploy.actions` | — | `list` | optional | same fields as `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.actions` | Optional. A sequence of Skaffold custom actions to invoke during execution of the postdeploy job. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].postdeploy.tasks` | — | `list` | optional | — | Optional. The tasks that will run as a part of the postdeploy job. The tasks are executed sequentially in the order specified. Only one of `actions` or `tasks` can be specified. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].postdeploy.tasks[]` | — | `map` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].postdeploy.tasks[].container` | — | `map` | optional | opaque | Optional. This task is represented by a container that is executed in the Cloud Build execution environment. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].predeploy` | — | `map` | optional | — | Optional. Configuration for the predeploy job of this phase. If this is not configured, there will be no predeploy job for this phase. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].predeploy.actions` | — | `list` | optional | same fields as `serialPipeline.stages[].strategy.canary.canaryDeployment.predeploy.actions` | Optional. A sequence of Skaffold custom actions to invoke during execution of the predeploy job. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].predeploy.tasks` | — | `list` | optional | — | Optional. The tasks that will run as a part of the predeploy job. The tasks are executed sequentially in the order specified. Only one of `actions` or `tasks` can be specified. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].predeploy.tasks[]` | — | `map` | optional | same fields as `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].postdeploy.tasks[]` | — |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].profiles` | — | `list` | optional | — | Optional. Skaffold profiles to use when rendering the manifest for this phase. These are in addition to the profiles list specified in the `DeliveryPipeline` stage. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].profiles[]` | — | `string` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].verify` | — | `boolean` | optional | — | Optional. Whether to run verify tests after the deployment via `skaffold verify`. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].verifyConfig` | `verify_config` | `map` | optional | — | Optional. Configuration for the verify job. Cannot be set if `verify` is set to true. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].verifyConfig.tasks` | — | `list` | optional | — | Optional. The tasks that will run as a part of the verify job. The tasks are executed sequentially in the order specified. |
| `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].verifyConfig.tasks[]` | — | `map` | optional | same fields as `serialPipeline.stages[].strategy.canary.customCanaryDeployment.phaseConfigs[].postdeploy.tasks[]` | — |
| `serialPipeline.stages[].strategy.canary.runtimeConfig` | `runtime_config` | `map` | optional | — | Optional. Runtime specific configurations for the deployment strategy. The runtime configuration is used to determine how Cloud Deploy will split traffic to enable a progressive deployment. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.cloudRun` | `cloud_run` | `map` | optional | — | Optional. Cloud Run runtime configuration. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.cloudRun.automaticTrafficControl` | `automatic_traffic_control` | `boolean` | optional | — | Optional. Whether Cloud Deploy should update the traffic stanza in a Cloud Run Service on the user's behalf to facilitate traffic splitting. This is required to be true for CanaryDeployments, but … |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.cloudRun.canaryRevisionTags` | `canary_revision_tags` | `list` | optional | — | Optional. A list of tags that are added to the canary revision while the canary phase is in progress. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.cloudRun.canaryRevisionTags[]` | — | `string` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.cloudRun.priorRevisionTags` | `prior_revision_tags` | `list` | optional | — | Optional. A list of tags that are added to the prior revision while the canary phase is in progress. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.cloudRun.priorRevisionTags[]` | — | `string` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.cloudRun.stableRevisionTags` | `stable_revision_tags` | `list` | optional | — | Optional. A list of tags that are added to the final stable revision when the stable phase is applied. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.cloudRun.stableRevisionTags[]` | — | `string` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes` | — | `map` | optional | — | Optional. Kubernetes runtime configuration. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.gatewayServiceMesh` | `gateway_service_mesh` | `map` | optional | — | Optional. Kubernetes Gateway API service mesh configuration. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.gatewayServiceMesh.deployment` | — | `string` | optional | — | Required. Name of the Kubernetes Deployment whose traffic is managed by the specified HTTPRoute and Service. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.gatewayServiceMesh.httpRoute` | `http_route` | `string` | optional | — | Required. Name of the Gateway API HTTPRoute. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.gatewayServiceMesh.podSelectorLabel` | `pod_selector_label` | `string` | optional | — | Optional. The label to use when selecting Pods for the Deployment and Service resources. This label must already be present in both resources. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.gatewayServiceMesh.routeDestinations` | `route_destinations` | `map` | optional | — | Optional. Route destinations allow configuring the Gateway API HTTPRoute to be deployed to additional clusters. This option is available for multi-cluster service mesh set ups that require the route … |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.gatewayServiceMesh.routeDestinations.destinationIds` | `destination_ids` | `list` | optional | — | Required. The clusters where the Gateway API HTTPRoute resource will be deployed to. Valid entries include the associated entities IDs configured in the Target resource and "@self" to include the … |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.gatewayServiceMesh.routeDestinations.destinationIds[]` | — | `string` | optional | — | — |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.gatewayServiceMesh.routeDestinations.propagateService` | `propagate_service` | `boolean` | optional | — | Optional. Whether to propagate the Kubernetes Service to the route destination clusters. The Service will always be deployed to the Target cluster even if the HTTPRoute is not. This option may be … |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.gatewayServiceMesh.routeUpdateWaitTime` | `route_update_wait_time` | `string` | optional | — | Optional. The time to wait for route updates to propagate. The maximum configurable time is 3 hours, in seconds format. If unspecified, there is no wait time. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.gatewayServiceMesh.service` | — | `string` | optional | — | Required. Name of the Kubernetes Service. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.gatewayServiceMesh.stableCutbackDuration` | `stable_cutback_duration` | `string` | optional | — | Optional. The amount of time to migrate traffic back from the canary Service to the original Service during the stable phase deployment. If specified, must be between 15s and 3600s. If unspecified … |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.serviceNetworking` | `service_networking` | `map` | optional | — | Optional. Kubernetes Service networking configuration. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.serviceNetworking.deployment` | — | `string` | optional | — | Required. Name of the Kubernetes Deployment whose traffic is managed by the specified Service. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.serviceNetworking.disablePodOverprovisioning` | `disable_pod_overprovisioning` | `boolean` | optional | — | Optional. Whether to disable Pod overprovisioning. If Pod overprovisioning is disabled then Cloud Deploy will limit the number of total Pods used for the deployment strategy to the number of Pods the … |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.serviceNetworking.podSelectorLabel` | `pod_selector_label` | `string` | optional | — | Optional. The label to use when selecting Pods for the Deployment resource. This label must already be present in the Deployment. |
| `serialPipeline.stages[].strategy.canary.runtimeConfig.kubernetes.serviceNetworking.service` | — | `string` | optional | — | Required. Name of the Kubernetes Service. |
| `serialPipeline.stages[].strategy.standard` | — | `map` | optional | — | Optional. Standard deployment strategy executes a single deploy and allows verifying the deployment. |
| `serialPipeline.stages[].strategy.standard.analysis` | — | `map` | optional | — | Optional. Configuration for the analysis job. If this is not configured, the analysis job will not be present. |
| `serialPipeline.stages[].strategy.standard.analysis.customChecks` | `custom_checks` | `list` | optional | — | Optional. Custom analysis checks from 3P metric providers. |
| `serialPipeline.stages[].strategy.standard.analysis.customChecks[]` | — | `map` | optional | — | — |
| `serialPipeline.stages[].strategy.standard.analysis.customChecks[].frequency` | — | `string` | optional | — | Optional. The frequency at which the custom check will be run, with a minimum and default of 5 minutes. |
| `serialPipeline.stages[].strategy.standard.analysis.customChecks[].id` | — | `string` | optional | — | Required. The ID of the custom Analysis check. |
| `serialPipeline.stages[].strategy.standard.analysis.customChecks[].task` | — | `map` | optional | — | Required. The Task to be run for this custom check. |
| `serialPipeline.stages[].strategy.standard.analysis.customChecks[].task.container` | — | `map` | optional | same fields as `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.tasks[].container` | Optional. This task is represented by a container that is executed in the Cloud Build execution environment. |
| `serialPipeline.stages[].strategy.standard.analysis.duration` | — | `string` | optional | — | Required. The amount of time in minutes the analysis on the target will last. If all analysis checks have successfully completed before the specified duration, the analysis is successful. If a check … |
| `serialPipeline.stages[].strategy.standard.analysis.googleCloud` | `google_cloud` | `map` | optional | same fields as `serialPipeline.stages[].strategy.canary.canaryDeployment.analysis.googleCloud` | Optional. Google Cloud - based analysis checks. |
| `serialPipeline.stages[].strategy.standard.postdeploy` | — | `map` | optional | — | Optional. Configuration for the postdeploy job. If this is not configured, the postdeploy job will not be present. |
| `serialPipeline.stages[].strategy.standard.postdeploy.actions` | — | `list` | optional | same fields as `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.actions` | Optional. A sequence of Skaffold custom actions to invoke during execution of the postdeploy job. |
| `serialPipeline.stages[].strategy.standard.postdeploy.tasks` | — | `list` | optional | same fields as `serialPipeline.stages[].strategy.canary.canaryDeployment.postdeploy.tasks` | Optional. The tasks that will run as a part of the postdeploy job. The tasks are executed sequentially in the order specified. Only one of `actions` or `tasks` can be specified. |
| `serialPipeline.stages[].strategy.standard.predeploy` | — | `map` | optional | — | Optional. Configuration for the predeploy job. If this is not configured, the predeploy job will not be present. |
| `serialPipeline.stages[].strategy.standard.predeploy.actions` | — | `list` | optional | same fields as `serialPipeline.stages[].strategy.canary.canaryDeployment.predeploy.actions` | Optional. A sequence of Skaffold custom actions to invoke during execution of the predeploy job. |
| `serialPipeline.stages[].strategy.standard.predeploy.tasks` | — | `list` | optional | same fields as `serialPipeline.stages[].strategy.canary.canaryDeployment.predeploy.tasks` | Optional. The tasks that will run as a part of the predeploy job. The tasks are executed sequentially in the order specified. Only one of `actions` or `tasks` can be specified. |
| `serialPipeline.stages[].strategy.standard.verify` | — | `boolean` | optional | — | Optional. Whether to verify a deployment via `skaffold verify`. |
| `serialPipeline.stages[].strategy.standard.verifyConfig` | `verify_config` | `map` | optional | same fields as `serialPipeline.stages[].strategy.canary.canaryDeployment.verifyConfig` | Optional. Configuration for the verify job. Cannot be set if `verify` is set to true. |
| `serialPipeline.stages[].targetId` | `target_id` | `string` | optional | — | Optional. The target_id to which this stage points. This field refers exclusively to the last segment of a target name. For example, this field would just be `my-target` (rather than … |
| `suspended` | — | `boolean` | optional | — | Optional. When suspended, no new releases or rollouts can be created, but in-progress ones will complete. |
| `uid` | — | `string` | output only | — | Output only. Unique identifier of the `DeliveryPipeline`. |
| `updateTime` | `update_time` | `string` | output only | — | Output only. Most recent time at which the pipeline was updated. |
