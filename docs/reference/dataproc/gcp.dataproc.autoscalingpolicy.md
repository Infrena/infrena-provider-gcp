<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.dataproc.autoscalingpolicy`

Describes an autoscaling policy for Dataproc cluster autoscaler.

[dataproc](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | dataproc |
| Scope | regional |
| Asset type | `dataproc.googleapis.com/AutoscalingPolicy` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 60s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/locations/{{location}}/autoscalingPolicies` |
| Read | yes | `GET projects/{{project}}/locations/{{location}}/autoscalingPolicies/{{policy_id}}` |
| Update | **no** | publishes no update method; every change to it replaces the resource |
| Delete | yes | `DELETE projects/{{project}}/locations/{{location}}/autoscalingPolicies/{{policy_id}}` |
| Import | yes | by id, see below |

## Import id

```
projects/{{project}}/locations/{{location}}/autoscalingPolicies/{{policy_id}}
```

## Attributes

27 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `basicAlgorithm` | `basic_algorithm` | `map` | optional | — | Basic algorithm for autoscaling. |
| `basicAlgorithm.cooldownPeriod` | `cooldown_period` | `string` | optional | — | Optional. Duration between scaling events. A scaling period starts after the update operation from the previous event has completed.Bounds: 2m, 1d. Default: 2m. |
| `basicAlgorithm.sparkStandaloneConfig` | `spark_standalone_config` | `map` | optional | — | Optional. Spark Standalone autoscaling configuration |
| `basicAlgorithm.sparkStandaloneConfig.gracefulDecommissionTimeout` | `graceful_decommission_timeout` | `string` | optional | — | Required. Timeout for Spark graceful decommissioning of spark workers. Specifies the duration to wait for spark worker to complete spark decommissioning tasks before forcefully removing workers. Only … |
| `basicAlgorithm.sparkStandaloneConfig.removeOnlyIdleWorkers` | `remove_only_idle_workers` | `boolean` | optional | — | Optional. Remove only idle workers when scaling down cluster |
| `basicAlgorithm.sparkStandaloneConfig.scaleDownFactor` | `scale_down_factor` | `float` | optional | — | Required. Fraction of required executors to remove from Spark Serverless clusters. A scale-down factor of 1.0 will result in scaling down so that there are no more executors for the Spark Job.(more … |
| `basicAlgorithm.sparkStandaloneConfig.scaleDownMinWorkerFraction` | `scale_down_min_worker_fraction` | `float` | optional | — | Optional. Minimum scale-down threshold as a fraction of total cluster size before scaling occurs. For example, in a 20-worker cluster, a threshold of 0.1 means the autoscaler must recommend at least … |
| `basicAlgorithm.sparkStandaloneConfig.scaleUpFactor` | `scale_up_factor` | `float` | optional | — | Required. Fraction of required workers to add to Spark Standalone clusters. A scale-up factor of 1.0 will result in scaling up so that there are no more required workers for the Spark Job (more … |
| `basicAlgorithm.sparkStandaloneConfig.scaleUpMinWorkerFraction` | `scale_up_min_worker_fraction` | `float` | optional | — | Optional. Minimum scale-up threshold as a fraction of total cluster size before scaling occurs. For example, in a 20-worker cluster, a threshold of 0.1 means the autoscaler must recommend at least a … |
| `basicAlgorithm.yarnConfig` | `yarn_config` | `map` | required | — | Optional. YARN autoscaling configuration. |
| `basicAlgorithm.yarnConfig.gracefulDecommissionTimeout` | `graceful_decommission_timeout` | `string` | required | — | Required. Timeout for YARN graceful decommissioning of Node Managers. Specifies the duration to wait for jobs to complete before forcefully removing workers (and potentially interrupting jobs). Only … |
| `basicAlgorithm.yarnConfig.scaleDownFactor` | `scale_down_factor` | `float` | required | — | Required. Fraction of average YARN pending memory in the last cooldown period for which to remove workers. A scale-down factor of 1 will result in scaling down so that there is no available memory … |
| `basicAlgorithm.yarnConfig.scaleDownMinWorkerFraction` | `scale_down_min_worker_fraction` | `float` | optional | — | Optional. Minimum scale-down threshold as a fraction of total cluster size before scaling occurs. For example, in a 20-worker cluster, a threshold of 0.1 means the autoscaler must recommend at least … |
| `basicAlgorithm.yarnConfig.scaleUpFactor` | `scale_up_factor` | `float` | required | — | Required. Fraction of average YARN pending memory in the last cooldown period for which to add workers. A scale-up factor of 1.0 will result in scaling up so that there is no pending memory remaining … |
| `basicAlgorithm.yarnConfig.scaleUpMinWorkerFraction` | `scale_up_min_worker_fraction` | `float` | optional | — | Optional. Minimum scale-up threshold as a fraction of total cluster size before scaling occurs. For example, in a 20-worker cluster, a threshold of 0.1 means the autoscaler must recommend at least a … |
| `clusterType` | `cluster_type` | `string` | optional | — | Optional. The type of the clusters for which this autoscaling policy is to be configured. |
| `id` | — | `string` | required | force new | Required. The policy id.The id must contain only letters (a-z, A-Z), numbers (0-9), underscores (_), and hyphens (-). Cannot begin or end with underscore or hyphen. Must consist of between 3 and 50 … |
| `labels` | — | `map` | optional | opaque | Optional. The labels to associate with this autoscaling policy. Label keys must contain 1 to 63 characters, and must conform to RFC 1035 (https://www.ietf.org/rfc/rfc1035.txt). Label values may be … |
| `name` | — | `string` | output only | — | Output only. The "resource name" of the autoscaling policy, as described in https://cloud.google.com/apis/design/resource_names. For projects.regions.autoscalingPolicies, the resource name of the … |
| `secondaryWorkerConfig` | `secondary_worker_config` | `map` | optional | — | Optional. Describes how the autoscaler will operate for secondary workers. |
| `secondaryWorkerConfig.maxInstances` | `max_instances` | `integer` | optional | — | Required. Maximum number of instances for this group. Required for primary workers. Note that by default, clusters will not use secondary workers. Required for secondary workers if the minimum … |
| `secondaryWorkerConfig.minInstances` | `min_instances` | `integer` | optional | — | Optional. Minimum number of instances for this group.Primary workers - Bounds: 2, max_instances. Default: 2. Secondary workers - Bounds: 0, max_instances. Default: 0. |
| `secondaryWorkerConfig.weight` | — | `integer` | optional | — | Optional. Weight for the instance group, which is used to determine the fraction of total workers in the cluster from this instance group. For example, if primary workers have weight 2, and secondary … |
| `workerConfig` | `worker_config` | `map` | optional | — | Required. Describes how the autoscaler will operate for primary workers. |
| `workerConfig.maxInstances` | `max_instances` | `integer` | required | — | Required. Maximum number of instances for this group. Required for primary workers. Note that by default, clusters will not use secondary workers. Required for secondary workers if the minimum … |
| `workerConfig.minInstances` | `min_instances` | `integer` | optional | — | Optional. Minimum number of instances for this group.Primary workers - Bounds: 2, max_instances. Default: 2. Secondary workers - Bounds: 0, max_instances. Default: 0. |
| `workerConfig.weight` | — | `integer` | optional | — | Optional. Weight for the instance group, which is used to determine the fraction of total workers in the cluster from this instance group. For example, if primary workers have weight 2, and secondary … |
