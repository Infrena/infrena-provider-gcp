<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.workflow`

Workflow program to be executed by Workflows.

[workflows](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | workflows |
| Scope | regional |
| Asset type | `workflows.googleapis.com/Workflow` |
| Hangs off | `projects` |
| Tier | 1 (ruled: encoder (encoders/workflow.go.tmpl) picks a name from name, name_prefix or a random id, d.Sets it, and returns the body unchanged; the name travels as the workflowId query parameter. This provider does not invent names, so name is required. pre_delete (pre_delete/workflows_workflow.go.tmpl) refuses when Terraform's deletion_protection is set; the v1 Workflow has no such field.
) |
| Mutation timeout | 1200s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/locations/{{region}}/workflows?workflowId={{name}}` |
| Read | yes | `GET projects/{{project}}/locations/{{region}}/workflows/{{name}}` |
| Update | yes | `PATCH projects/{{project}}/locations/{{region}}/workflows/{{name}}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE projects/{{project}}/locations/{{region}}/workflows/{{name}}` |
| Import | yes | by id, see below |

## Import id

```
projects/{{project}}/locations/{{region}}/workflows/{{name}}
```

## Attributes

23 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `allKmsKeys` | `all_kms_keys` | `list` | output only | — | Output only. A list of all KMS crypto keys used to encrypt or decrypt the data associated with the workflow. |
| `allKmsKeys[]` | — | `string` | optional | — | — |
| `allKmsKeysVersions` | `all_kms_keys_versions` | `list` | output only | — | Output only. A list of all KMS crypto key versions used to encrypt or decrypt the data associated with the workflow. |
| `allKmsKeysVersions[]` | — | `string` | optional | — | — |
| `callLogLevel` | `call_log_level` | `string` | optional | — | Optional. Describes the level of platform logging to apply to calls and call responses during executions of this workflow. If both the workflow and the execution specify a logging level, the … |
| `createTime` | `create_time` | `string` | output only | — | Output only. The timestamp for when the workflow was created. This is a workflow-wide field and is not tied to a specific revision. |
| `cryptoKeyName` | `crypto_key_name` | `string` | optional | — | Optional. The resource name of a KMS crypto key used to encrypt or decrypt the data associated with the workflow. Format … |
| `cryptoKeyVersion` | `crypto_key_version` | `string` | output only | — | Output only. The resource name of a KMS crypto key version used to encrypt or decrypt the data associated with the workflow. Format … |
| `description` | — | `string` | optional | — | Description of the workflow provided by the user. Must be at most 1000 Unicode characters long. This is a workflow-wide field and is not tied to a specific revision. |
| `executionHistoryLevel` | `execution_history_level` | `string` | optional | — | Optional. Describes the execution history level to apply to this workflow. |
| `labels` | — | `map` | optional | opaque | Labels associated with this workflow. Labels can contain at most 64 entries. Keys and values can be no longer than 63 characters and can only contain lowercase letters, numeric characters … |
| `name` | — | `string` | required | force new | The resource name of the workflow. Format: projects/{project}/locations/{location}/workflows/{workflow}. This is a workflow-wide field and is not tied to a specific revision. |
| `revisionCreateTime` | `revision_create_time` | `string` | output only | — | Output only. The timestamp for the latest revision of the workflow's creation. |
| `revisionId` | `revision_id` | `string` | output only | — | Output only. The revision of the workflow. A new revision of a workflow is created as a result of updating the following properties of a workflow: - Service account - Workflow code to be executed The … |
| `serviceAccount` | `service_account` | `string` | optional | — | The service account associated with the latest workflow version. This service account represents the identity of the workflow and determines what permissions the workflow has. Format … |
| `sourceContents` | `source_contents` | `string` | required | — | Workflow code to be executed. The size limit is 128KB. |
| `state` | — | `string` | output only | — | Output only. State of the workflow deployment. |
| `stateError` | `state_error` | `map` | output only | — | Output only. Error regarding the state of the workflow. For example, this field will have error details if the execution data is unavailable due to revoked KMS key permissions. |
| `stateError.details` | — | `string` | optional | — | Provides specifics about the error. |
| `stateError.type` | — | `string` | optional | — | The type of this state error. |
| `tags` | — | `map` | optional | force new, opaque | Optional. Input only. Immutable. Tags associated with this workflow. |
| `updateTime` | `update_time` | `string` | output only | — | Output only. The timestamp for when the workflow was last updated. This is a workflow-wide field and is not tied to a specific revision. |
| `userEnvVars` | `user_env_vars` | `map` | optional | opaque | Optional. User-defined environment variables associated with this workflow revision. This map has a maximum length of 20. Each string can take up to 4KiB. Keys cannot be empty strings and cannot … |
