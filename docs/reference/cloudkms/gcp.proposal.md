<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.proposal`

A SingleTenantHsmInstanceProposal represents a proposal to perform an operation on a SingleTenantHsmInstance.

[cloudkms](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | cloudkms |
| Scope | global |
| Asset type | `cloudkms.googleapis.com/SingleTenantHsmInstanceProposal` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 1200s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | **no** | the create url `{+parent}/proposals` needs `singleTenantHsmInstance`, which nothing supplies: no provider setting (project, region, zone, location), no stored binding, and no settable attribute of this type |
| Read | yes | `GET {+name}` |
| Update | **no** | publishes no update method; every change to it replaces the resource |
| Delete | yes | `DELETE {+name}` |
| Import | yes | by id, see below |

## Import id

```
projects/{project}/locations/{location}/singleTenantHsmInstances/{singleTenantHsmInstance}/proposals/{proposal}
```

## Attributes

42 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `addQuorumMember` | `add_quorum_member` | `map` | optional | — | Add a quorum member to the SingleTenantHsmInstance. This will increase the total_approver_count by 1. The SingleTenantHsmInstance must be in the ACTIVE state to perform this operation. |
| `addQuorumMember.twoFactorPublicKeyPem` | `two_factor_public_key_pem` | `string` | optional | — | Required. The public key associated with the 2FA key for the new quorum member to add. Public keys must be associated with RSA 2048 keys. |
| `createTime` | `create_time` | `string` | output only | — | Output only. The time at which the SingleTenantHsmInstanceProposal was created. |
| `deleteSingleTenantHsmInstance` | `delete_single_tenant_hsm_instance` | `map` | optional | opaque | Delete the SingleTenantHsmInstance. Deleting a SingleTenantHsmInstance will make all CryptoKeys attached to the SingleTenantHsmInstance unusable. The SingleTenantHsmInstance must be in the DISABLED … |
| `deleteTime` | `delete_time` | `string` | output only | — | Output only. The time at which the SingleTenantHsmInstanceProposal was deleted. |
| `disableSingleTenantHsmInstance` | `disable_single_tenant_hsm_instance` | `map` | optional | opaque | Disable the SingleTenantHsmInstance. The SingleTenantHsmInstance must be in the ACTIVE state to perform this operation. |
| `enableSingleTenantHsmInstance` | `enable_single_tenant_hsm_instance` | `map` | optional | opaque | Enable the SingleTenantHsmInstance. The SingleTenantHsmInstance must be in the DISABLED state to perform this operation. |
| `expireTime` | `expire_time` | `string` | optional | — | The time at which the SingleTenantHsmInstanceProposal will expire if not approved and executed. |
| `failureReason` | `failure_reason` | `string` | output only | — | Output only. The root cause of the most recent failure. Only present if state is FAILED. |
| `name` | — | `string` | optional | — | Identifier. The resource name for this SingleTenantHsmInstance in the format `projects/*/locations/*/singleTenantHsmInstances/*/proposals/*`. |
| `purgeTime` | `purge_time` | `string` | output only | — | Output only. The time at which the soft-deleted SingleTenantHsmInstanceProposal will be permanently purged. This field is only populated when the state is DELETED and will be set a time after … |
| `quorumParameters` | `quorum_parameters` | `map` | output only | — | Output only. The quorum approval parameters for the SingleTenantHsmInstanceProposal. |
| `quorumParameters.approvedTwoFactorPublicKeyPems` | `approved_two_factor_public_key_pems` | `list` | output only | — | Output only. The public keys associated with the 2FA keys that have already approved the SingleTenantHsmInstanceProposal by signing the challenge. |
| `quorumParameters.approvedTwoFactorPublicKeyPems[]` | — | `string` | optional | — | — |
| `quorumParameters.challenges` | — | `list` | output only | — | Output only. The challenges to be signed by 2FA keys for quorum auth. M of N of these challenges are required to be signed to approve the operation. |
| `quorumParameters.challenges[]` | — | `map` | optional | — | — |
| `quorumParameters.challenges[].challenge` | — | `string` | output only | — | Output only. The challenge to be signed by the 2FA key indicated by the public key. |
| `quorumParameters.challenges[].publicKeyPem` | `public_key_pem` | `string` | output only | — | Output only. The public key associated with the 2FA key that should sign the challenge. |
| `quorumParameters.requiredApproverCount` | `required_approver_count` | `integer` | output only | — | Output only. The required numbers of approvers. This is the M value used for M of N quorum auth. It is less than the number of public keys. |
| `refreshSingleTenantHsmInstance` | `refresh_single_tenant_hsm_instance` | `map` | optional | opaque | Refreshes the SingleTenantHsmInstance. This operation must be performed periodically to keep the SingleTenantHsmInstance active. This operation must be performed before … |
| `registerTwoFactorAuthKeys` | `register_two_factor_auth_keys` | `map` | optional | — | Register 2FA keys for the SingleTenantHsmInstance. This operation requires all N Challenges to be signed by 2FA keys. The SingleTenantHsmInstance must be in the PENDING_TWO_FACTOR_AUTH_REGISTRATION … |
| `registerTwoFactorAuthKeys.requiredApproverCount` | `required_approver_count` | `integer` | optional | — | Required. The required numbers of approvers to set for the SingleTenantHsmInstance. This is the M value used for M of N quorum auth. Must be greater than or equal to 2 and less than or equal to … |
| `registerTwoFactorAuthKeys.twoFactorPublicKeyPems` | `two_factor_public_key_pems` | `list` | optional | — | Required. The public keys associated with the 2FA keys for M of N quorum auth. Public keys must be associated with RSA 2048 keys. |
| `registerTwoFactorAuthKeys.twoFactorPublicKeyPems[]` | — | `string` | optional | — | — |
| `removeQuorumMember` | `remove_quorum_member` | `map` | optional | — | Remove a quorum member from the SingleTenantHsmInstance. This will reduce total_approver_count by 1. The SingleTenantHsmInstance must be in the ACTIVE state to perform this operation. |
| `removeQuorumMember.twoFactorPublicKeyPem` | `two_factor_public_key_pem` | `string` | optional | — | Required. The public key associated with the 2FA key for the quorum member to remove. Public keys must be associated with RSA 2048 keys. |
| `requiredActionQuorumParameters` | `required_action_quorum_parameters` | `map` | output only | — | Output only. Parameters for an approval of a SingleTenantHsmInstanceProposal that has both required challenges and a quorum. |
| `requiredActionQuorumParameters.approvedTwoFactorPublicKeyPems` | `approved_two_factor_public_key_pems` | `list` | output only | same fields as `quorumParameters.approvedTwoFactorPublicKeyPems` | Output only. The public keys associated with the 2FA keys that have already approved the SingleTenantHsmInstanceProposal by signing the challenge. |
| `requiredActionQuorumParameters.quorumChallenges` | `quorum_challenges` | `list` | output only | — | Output only. The challenges to be signed by 2FA keys for quorum auth. M of N of these challenges are required to be signed to approve the operation. |
| `requiredActionQuorumParameters.quorumChallenges[]` | — | `map` | optional | — | — |
| `requiredActionQuorumParameters.quorumChallenges[].challenge` | — | `string` | output only | — | Output only. The challenge to be signed by the 2FA key indicated by the public key. |
| `requiredActionQuorumParameters.quorumChallenges[].publicKeyPem` | `public_key_pem` | `string` | output only | — | Output only. The public key associated with the 2FA key that should sign the challenge. |
| `requiredActionQuorumParameters.requiredApproverCount` | `required_approver_count` | `integer` | output only | — | Output only. The required number of quorum approvers. This is the M value used for M of N quorum auth. It is less than the number of public keys. |
| `requiredActionQuorumParameters.requiredChallenges` | `required_challenges` | `list` | output only | — | Output only. A list of specific challenges that must be signed. For some operations, this will contain a single challenge. |
| `requiredActionQuorumParameters.requiredChallenges[]` | — | `map` | optional | — | — |
| `requiredActionQuorumParameters.requiredChallenges[].challenge` | — | `string` | output only | — | Output only. The challenge to be signed by the 2FA key indicated by the public key. |
| `requiredActionQuorumParameters.requiredChallenges[].publicKeyPem` | `public_key_pem` | `string` | output only | — | Output only. The public key associated with the 2FA key that should sign the challenge. |
| `state` | — | `string` | output only | — | Output only. The state of the SingleTenantHsmInstanceProposal. |
| `ttl` | — | `string` | optional | — | Input only. The TTL for the SingleTenantHsmInstanceProposal. Proposals will expire after this duration. |
| `upgradeKeyTrust` | `upgrade_key_trust` | `map` | optional | — | Promotes a key with the AES_WRAPPING purpose to a trusted wrapping key. The key must be in the ACTIVE state to perform this operation. |
| `upgradeKeyTrust.name` | — | `string` | optional | — | Required. The name of the CryptoKeyVersion to promote. |
| `upgradeKeyTrust.twoFactorPublicKeyPem` | `two_factor_public_key_pem` | `string` | optional | — | Required. The public key associated with the 2FA key that will sign the login nonce for this operation. |
