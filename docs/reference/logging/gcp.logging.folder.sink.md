<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.logging.folder.sink`

Describes a sink used to export log entries to one of the following destinations: a Cloud Logging log bucket, a Cloud Storage bucket, a BigQuery dataset, a Pub/Sub topic, a Cloud project.A logs filter controls which log entries are exported. The sink must be created within a project, organization, billing account, or folder.

[logging](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | logging |
| Scope | global |
| Asset type | `logging.googleapis.com/LogSink` |
| Hangs off | `folders` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 60s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | **no** | the create url `{+parent}/sinks` needs `folder`, which nothing supplies: no provider setting (project, region, zone, location), no stored binding, and no settable attribute of this type |
| Read | yes | `GET {+sinkName}` |
| Update | yes | `PATCH {+sinkName}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE {+sinkName}` |
| Import | yes | by id, see below |

## Import id

```
{+sinkName}
```

## Attributes

23 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `bigqueryOptions` | `bigquery_options` | `map` | optional | — | Optional. Options that affect sinks exporting data to BigQuery. |
| `bigqueryOptions.usePartitionedTables` | `use_partitioned_tables` | `boolean` | optional | — | Optional. Whether to use BigQuery's partition tables (https://docs.cloud.google.com/bigquery/docs/partitioned-tables). By default, Cloud Logging creates dated tables based on the log entries' … |
| `bigqueryOptions.usesTimestampColumnPartitioning` | `uses_timestamp_column_partitioning` | `boolean` | output only | — | Output only. True if new timestamp column based partitioning is in use, false if legacy ingress-time partitioning is in use.All new sinks will have this field set true and will use timestamp column … |
| `createTime` | `create_time` | `string` | output only | — | Output only. The creation timestamp of the sink.This field may not be present for older sinks. |
| `description` | — | `string` | optional | — | Optional. A description of this sink.The maximum length of the description is 8000 characters. |
| `destination` | — | `string` | optional | — | Required. The export destination: "storage.googleapis.com/\[GCS_BUCKET\]" "bigquery.googleapis.com/projects/\[PROJECT_ID\]/datasets/\[DATASET\]" … |
| `disabled` | — | `boolean` | optional | — | Optional. If set to true, then this sink is disabled and it does not export any log entries. |
| `exclusions` | — | `list` | optional | — | Optional. Log entries that match any of these exclusion filters will not be exported.If a log entry is matched by both filter and one of exclusions it will not be exported. |
| `exclusions[]` | — | `map` | optional | — | — |
| `exclusions[].createTime` | `create_time` | `string` | output only | — | Output only. The creation timestamp of the exclusion.This field may not be present for older exclusions. |
| `exclusions[].description` | — | `string` | optional | — | Optional. A description of this exclusion. |
| `exclusions[].disabled` | — | `boolean` | optional | — | Optional. If set to True, then this exclusion is disabled and it does not exclude any log entries. You can update an exclusion to change the value of this field. |
| `exclusions[].filter` | — | `string` | optional | — | Required. An advanced logs filter (https://docs.cloud.google.com/logging/docs/view/building-queries#queries-by-expression) that matches the log entries to be excluded. By using the sample function … |
| `exclusions[].name` | — | `string` | optional | — | Optional. A client-assigned identifier, such as "load-balancer-exclusion". Identifiers are limited to 100 characters and can include only letters, digits, underscores, hyphens, and periods. First … |
| `exclusions[].updateTime` | `update_time` | `string` | output only | — | Output only. The last update timestamp of the exclusion.This field may not be present for older exclusions. |
| `filter` | — | `string` | optional | — | Optional. An advanced logs filter (https://docs.cloud.google.com/logging/docs/view/building-queries#queries-by-expression). The only exported log entries are those that are in the resource owning the … |
| `includeChildren` | `include_children` | `boolean` | optional | — | Optional. This field applies only to sinks owned by organizations and folders. If the field is false, the default, only the logs owned by the sink's parent resource are available for export. If the … |
| `interceptChildren` | `intercept_children` | `boolean` | optional | — | Optional. This field applies only to sinks owned by organizations and folders.When the value of 'intercept_children' is true, the following restrictions apply: The sink must have the include_children … |
| `name` | — | `string` | optional | — | Optional. The client-assigned sink identifier, unique within the project.For example: "my-syslog-errors-to-pubsub".Sink identifiers are limited to 100 characters and can include only the following … |
| `outputVersionFormat` | `output_version_format` | `string` | optional | — | Deprecated. This field is unused. |
| `resourceName` | `resource_name` | `string` | output only | — | Output only. The resource name of the sink. "projects/\[PROJECT_ID\]/sinks/\[SINK_NAME\] "organizations/\[ORGANIZATION_ID\]/sinks/\[SINK_NAME\] … |
| `updateTime` | `update_time` | `string` | output only | — | Output only. The last update timestamp of the sink.This field may not be present for older sinks. |
| `writerIdentity` | `writer_identity` | `string` | output only | — | Output only. An IAM identity—a service account or group—under which Cloud Logging writes the exported log entries to the sink's destination. This field is either set by specifying … |
