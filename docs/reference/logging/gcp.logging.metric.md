<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.logging.metric`

Logs-based metric can also be used to extract values from logs and create a a distribution of the values. The distribution records the statistics of the extracted values along with an optional histogram of the values as specified by the bucket options.

[logging](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | logging |
| Scope | global |
| Asset type | `logging.googleapis.com/LogMetric` |
| Hangs off | `projects` |
| Tier | 1 (ruled: custom_import (custom_import/self_link_as_name.tmpl) only parses `terraform import`'s id argument, which infrena does not use: import here goes through the catalog's own id templates. Nothing on the wire.
) |
| Mutation timeout | 60s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/metrics` |
| Read | yes | `GET projects/{{project}}/metrics/{{%name}}` |
| Update | **no** | publishes no update method; every change to it replaces the resource |
| Delete | yes | `DELETE projects/{{project}}/metrics/{{%name}}` |
| Import | yes | by id, see below |

## Import id

Any one of these shapes is accepted, tried in order:

```
{{name}}
{{project}} {{name}}
```

## Attributes

45 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `bucketName` | `bucket_name` | `string` | optional | — | Optional. The resource name of the Log Bucket that owns the Log Metric. Only Log Buckets in projects are supported. The bucket has to be in the same project as the metric.For … |
| `bucketOptions` | `bucket_options` | `map` | optional | — | Optional. The bucket_options are required when the logs-based metric is using a DISTRIBUTION value type and it describes the bucket boundaries used to create a histogram of the extracted values. |
| `bucketOptions.explicitBuckets` | `explicit_buckets` | `map` | optional | — | The explicit buckets. |
| `bucketOptions.explicitBuckets.bounds` | — | `list` | required | — | The values must be monotonically increasing. |
| `bucketOptions.explicitBuckets.bounds[]` | — | `float` | optional | — | — |
| `bucketOptions.exponentialBuckets` | `exponential_buckets` | `map` | optional | — | The exponential buckets. |
| `bucketOptions.exponentialBuckets.growthFactor` | `growth_factor` | `float` | required | — | Must be greater than 1. |
| `bucketOptions.exponentialBuckets.numFiniteBuckets` | `num_finite_buckets` | `integer` | required | — | Must be greater than 0. |
| `bucketOptions.exponentialBuckets.scale` | — | `float` | required | — | Must be greater than 0. |
| `bucketOptions.linearBuckets` | `linear_buckets` | `map` | optional | — | The linear bucket. |
| `bucketOptions.linearBuckets.numFiniteBuckets` | `num_finite_buckets` | `integer` | required | — | Must be greater than 0. |
| `bucketOptions.linearBuckets.offset` | — | `float` | required | — | Lower bound of the first bucket. |
| `bucketOptions.linearBuckets.width` | — | `float` | required | — | Must be greater than 0. |
| `createTime` | `create_time` | `string` | output only | — | Output only. The creation timestamp of the metric.This field may not be present for older metrics. |
| `description` | — | `string` | optional | — | Optional. A description of this metric, which is used in documentation. The maximum length of the description is 8000 characters. |
| `disabled` | — | `boolean` | optional | — | Optional. If set to True, then this metric is disabled and it does not generate any points. |
| `filter` | — | `string` | required | — | Required. An advanced logs filter (https://cloud.google.com/logging/docs/view/advanced_filters) which is used to match log entries. Example: "resource.type=gae_app AND severity>=ERROR" The maximum … |
| `labelExtractors` | `label_extractors` | `map` | optional | opaque | Optional. A map from a label key string to an extractor expression which is used to extract data from a log entry field and assign as the label value. Each label key specified in the LabelDescriptor … |
| `metricDescriptor` | `metric_descriptor` | `map` | optional | — | Optional. The metric descriptor associated with the logs-based metric. If unspecified, it uses a default metric descriptor with a DELTA metric kind, INT64 value type, with no labels and a unit of … |
| `metricDescriptor.description` | — | `string` | optional | — | A detailed description of the metric, which can be used in documentation. |
| `metricDescriptor.displayName` | `display_name` | `string` | optional | — | A concise name for the metric, which can be displayed in user interfaces. Use sentence case without an ending period, for example "Request count". This field is optional but it is recommended to be … |
| `metricDescriptor.labels` | — | `list` | optional | unordered | The set of labels that can be used to describe a specific instance of this metric type. For example, the appengine.googleapis.com/http/server/response_latencies metric type has a label for the HTTP … |
| `metricDescriptor.labels[]` | — | `map` | optional | — | — |
| `metricDescriptor.labels[].description` | — | `string` | optional | — | A human-readable description for the label. |
| `metricDescriptor.labels[].key` | — | `string` | required | force new | The label key. |
| `metricDescriptor.labels[].valueType` | `value_type` | `string` | optional | force new | The type of data that can be assigned to the label. |
| `metricDescriptor.launchStage` | `launch_stage` | `string` | optional | — | Optional. The launch stage of the metric definition. |
| `metricDescriptor.metadata` | — | `map` | optional | — | Optional. Metadata which can be used to guide usage of the metric. |
| `metricDescriptor.metadata.ingestDelay` | `ingest_delay` | `string` | optional | — | The delay of data points caused by ingestion. Data points older than this age are guaranteed to be ingested and available to be read, excluding data loss due to errors. |
| `metricDescriptor.metadata.launchStage` | `launch_stage` | `string` | optional | — | Deprecated. Must use the MetricDescriptor.launch_stage instead. |
| `metricDescriptor.metadata.samplePeriod` | `sample_period` | `string` | optional | — | The sampling period of metric data points. For metrics which are written periodically, consecutive data points are stored at this time interval, excluding data loss due to errors. Metrics with a … |
| `metricDescriptor.metadata.timeSeriesResourceHierarchyLevel` | `time_series_resource_hierarchy_level` | `list` | optional | — | The scope of the timeseries data of the metric. |
| `metricDescriptor.metadata.timeSeriesResourceHierarchyLevel[]` | — | `string` | optional | — | — |
| `metricDescriptor.metricKind` | `metric_kind` | `string` | required | — | Whether the metric records instantaneous values, changes to a value, etc. Some combinations of metric_kind and value_type might not be supported. |
| `metricDescriptor.monitoredResourceTypes` | `monitored_resource_types` | `list` | optional | — | Read-only. If present, then a time series, which is identified partially by a metric type and a MonitoredResourceDescriptor, that is associated with this metric type can only be associated with one … |
| `metricDescriptor.monitoredResourceTypes[]` | — | `string` | optional | — | — |
| `metricDescriptor.name` | — | `string` | optional | — | The resource name of the metric descriptor. |
| `metricDescriptor.type` | — | `string` | optional | — | The metric type, including its DNS name prefix. The type is not URL-encoded. All user-defined metric types have the DNS name custom.googleapis.com or external.googleapis.com. Metric types should use … |
| `metricDescriptor.unit` | — | `string` | optional | — | The units in which the metric value is reported. It is only applicable if the value_type is INT64, DOUBLE, or DISTRIBUTION. The unit defines the representation of the stored metric values.Different … |
| `metricDescriptor.valueType` | `value_type` | `string` | required | — | Whether the measurement is an integer, a floating-point number, etc. Some combinations of metric_kind and value_type might not be supported. |
| `name` | — | `string` | required | — | Required. The client-assigned metric identifier. Examples: "error_count", "nginx/requests".Metric identifiers are limited to 100 characters and can include only the following characters: A-Z, a-z … |
| `resourceName` | `resource_name` | `string` | output only | — | Output only. The resource name of the metric: "projects/\[PROJECT_ID\]/metrics/\[METRIC_ID\]" |
| `updateTime` | `update_time` | `string` | output only | — | Output only. The last update timestamp of the metric.This field may not be present for older metrics. |
| `valueExtractor` | `value_extractor` | `string` | optional | — | Optional. A value_extractor is required when using a distribution logs-based metric to extract the values to record from a log entry. Two functions are supported for value extraction: EXTRACT(field) … |
| `version` | — | `string` | optional | — | Deprecated. The API version that created or updated this metric. The v2 format is used by default and cannot be changed. |
