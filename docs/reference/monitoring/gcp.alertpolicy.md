<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.alertpolicy`

A description of the conditions under which some aspect of your system is considered to be "unhealthy" and the ways to notify people or services about this state.

[monitoring](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | monitoring |
| Scope | global |
| Asset type | `monitoring.googleapis.com/AlertPolicy` |
| Hangs off | `projects` |
| Tier | 1 (ruled: custom_import (custom_import/self_link_as_name_set_project.go.tmpl) only parses `terraform import`'s id argument, which infrena does not use: import here goes through the catalog's own id templates. Nothing on the wire.
) |
| Mutation timeout | 60s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/alertPolicies` |
| Read | yes | `GET {+name}` |
| Update | yes | `PATCH {+name}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE {+name}` |
| Import | yes | by id, see below |

## Import id

```
projects/{{project}}/alertPolicies/{{name}}
```

## Attributes

109 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `alertStrategy` | `alert_strategy` | `map` | optional | — | Control over how this alerting policy's notification channels are notified. |
| `alertStrategy.autoClose` | `auto_close` | `string` | optional | — | If an alerting policy that was active has no data for this long, any open incidents will close |
| `alertStrategy.notificationChannelStrategy` | `notification_channel_strategy` | `list` | optional | — | Control how notifications will be sent out, on a per-channel basis. |
| `alertStrategy.notificationChannelStrategy[]` | — | `map` | optional | — | — |
| `alertStrategy.notificationChannelStrategy[].notificationChannelNames` | `notification_channel_names` | `list` | optional | — | The full REST resource name for the notification channels that these settings apply to. Each of these correspond to the name field in one of the NotificationChannel objects referenced in the … |
| `alertStrategy.notificationChannelStrategy[].notificationChannelNames[]` | — | `string` | optional | — | — |
| `alertStrategy.notificationChannelStrategy[].renotifyInterval` | `renotify_interval` | `string` | optional | — | The frequency at which to send reminder notifications for open incidents. The value must be between 30 minutes and 24 hours. |
| `alertStrategy.notificationPrompts` | `notification_prompts` | `list` | optional | — | For log-based alert policies, the notification prompts is always OPENED. For non log-based alert policies, the notification prompts can be OPENED or OPENED, CLOSED. |
| `alertStrategy.notificationPrompts[]` | — | `string` | optional | — | — |
| `alertStrategy.notificationRateLimit` | `notification_rate_limit` | `map` | optional | — | Required for log-based alerting policies, i.e. policies with a LogMatch condition.This limit is not implemented for alerting policies that do not have a LogMatch condition. |
| `alertStrategy.notificationRateLimit.period` | — | `string` | optional | — | Not more than one notification per period. |
| `combiner` | — | `string` | required | — | How to combine the results of multiple conditions to determine if an incident should be opened. If condition_time_series_query_language is present, this must be COMBINE_UNSPECIFIED. |
| `conditions` | — | `list` | required | — | A list of conditions for the policy. The conditions are combined by AND or OR according to the combiner field. If the combined conditions evaluate to true, then an incident is created. A policy can … |
| `conditions[]` | — | `map` | optional | — | — |
| `conditions[].conditionAbsent` | `condition_absent` | `map` | optional | — | A condition that checks that a time series continues to receive new data points. |
| `conditions[].conditionAbsent.aggregations` | — | `list` | optional | — | Specifies the alignment of data points in individual time series as well as how to combine the retrieved time series together (such as when aggregating multiple streams on each resource to a single … |
| `conditions[].conditionAbsent.aggregations[]` | — | `map` | optional | — | — |
| `conditions[].conditionAbsent.aggregations[].alignmentPeriod` | `alignment_period` | `string` | optional | — | The alignment_period specifies a time interval, in seconds, that is used to divide the data in all the time series into consistent blocks of time. This will be done before the per-series aligner can … |
| `conditions[].conditionAbsent.aggregations[].crossSeriesReducer` | `cross_series_reducer` | `string` | optional | — | The reduction operation to be used to combine time series into a single time series, where the value of each data point in the resulting series is a function of all the already aligned values in the … |
| `conditions[].conditionAbsent.aggregations[].groupByFields` | `group_by_fields` | `list` | optional | — | The set of fields to preserve when cross_series_reducer is specified. The group_by_fields determine how the time series are partitioned into subsets prior to applying the aggregation operation. Each … |
| `conditions[].conditionAbsent.aggregations[].groupByFields[]` | — | `string` | optional | — | — |
| `conditions[].conditionAbsent.aggregations[].perSeriesAligner` | `per_series_aligner` | `string` | optional | — | An Aligner describes how to bring the data points in a single time series into temporal alignment. Except for ALIGN_NONE, all alignments cause all the data points in an alignment_period to be … |
| `conditions[].conditionAbsent.duration` | — | `string` | required | — | Required. The amount of time that a time series must fail to report new data to be considered failing. The minimum value of this field is 120 seconds. Larger values that are a multiple of a … |
| `conditions[].conditionAbsent.filter` | — | `string` | optional | — | Required. A filter (https://cloud.google.com/monitoring/api/v3/filters) that identifies which time series should be compared with the threshold.The filter is similar to the one that is specified in … |
| `conditions[].conditionAbsent.trigger` | — | `map` | optional | — | The number/percent of time series for which the comparison must hold in order for the condition to trigger. If unspecified, then the condition will trigger if the comparison is true for any of the … |
| `conditions[].conditionAbsent.trigger.count` | — | `integer` | optional | — | The absolute number of time series that must fail the predicate for the condition to be triggered. |
| `conditions[].conditionAbsent.trigger.percent` | — | `float` | optional | — | The percentage of time series that must fail the predicate for the condition to be triggered. |
| `conditions[].conditionMatchedLog` | `condition_matched_log` | `map` | optional | — | A condition that checks for log messages matching given constraints. If set, no other conditions can be present. |
| `conditions[].conditionMatchedLog.filter` | — | `string` | required | — | Required. A logs-based filter. See Advanced Logs Queries (https://cloud.google.com/logging/docs/view/advanced-queries) for how this filter should be constructed. |
| `conditions[].conditionMatchedLog.labelExtractors` | `label_extractors` | `map` | optional | opaque | Optional. A map from a label key to an extractor expression, which is used to extract the value for this label key. Each entry in this map is a specification for how data should be extracted from log … |
| `conditions[].conditionMonitoringQueryLanguage` | `condition_monitoring_query_language` | `map` | optional | — | A condition that uses the Monitoring Query Language to define alerts. |
| `conditions[].conditionMonitoringQueryLanguage.duration` | — | `string` | required | — | Optional. The amount of time that a time series must violate the threshold to be considered failing. Currently, only values that are a multiple of a minute--e.g., 0, 60, 120, or 300 seconds--are … |
| `conditions[].conditionMonitoringQueryLanguage.evaluationMissingData` | `evaluation_missing_data` | `string` | optional | — | A condition control that determines how metric-threshold conditions are evaluated when data stops arriving. |
| `conditions[].conditionMonitoringQueryLanguage.query` | — | `string` | required | — | Monitoring Query Language (https://cloud.google.com/monitoring/mql) query that outputs a boolean stream. |
| `conditions[].conditionMonitoringQueryLanguage.trigger` | — | `map` | optional | — | The number/percent of time series for which the comparison must hold in order for the condition to trigger. If unspecified, then the condition will trigger if the comparison is true for any of the … |
| `conditions[].conditionMonitoringQueryLanguage.trigger.count` | — | `integer` | optional | — | The absolute number of time series that must fail the predicate for the condition to be triggered. |
| `conditions[].conditionMonitoringQueryLanguage.trigger.percent` | — | `float` | optional | — | The percentage of time series that must fail the predicate for the condition to be triggered. |
| `conditions[].conditionPrometheusQueryLanguage` | `condition_prometheus_query_language` | `map` | optional | — | A condition that uses the Prometheus query language to define alerts. |
| `conditions[].conditionPrometheusQueryLanguage.alertRule` | `alert_rule` | `string` | optional | — | Optional. The alerting rule name of this alert in the corresponding Prometheus configuration file.Some external tools may require this field to be populated correctly in order to refer to the … |
| `conditions[].conditionPrometheusQueryLanguage.disableMetricValidation` | `disable_metric_validation` | `boolean` | optional | — | Optional. Whether to disable metric existence validation for this condition.This allows alerting policies to be defined on metrics that do not yet exist, improving advanced customer workflows such as … |
| `conditions[].conditionPrometheusQueryLanguage.duration` | — | `string` | optional | — | Optional. Alerts are considered firing once their PromQL expression was evaluated to be "true" for this long. Alerts whose PromQL expression was not evaluated to be "true" for long enough are … |
| `conditions[].conditionPrometheusQueryLanguage.evaluationInterval` | `evaluation_interval` | `string` | optional | — | Optional. How often this rule should be evaluated. Must be a positive multiple of 30 seconds or missing. This field is optional. Its default value is 30 seconds. If this … |
| `conditions[].conditionPrometheusQueryLanguage.labels` | — | `map` | optional | opaque | Optional. Labels to add to or overwrite in the PromQL query result. Label names must be valid (https://prometheus.io/docs/concepts/data_model/#metric-names-and-labels). Label values can be … |
| `conditions[].conditionPrometheusQueryLanguage.query` | — | `string` | required | — | Required. The PromQL expression to evaluate. Every evaluation cycle this expression is evaluated at the current time, and all resultant time series become pending/firing alerts. This field must not … |
| `conditions[].conditionPrometheusQueryLanguage.ruleGroup` | `rule_group` | `string` | optional | — | Optional. The rule group name of this alert in the corresponding Prometheus configuration file.Some external tools may require this field to be populated correctly in order to refer to the original … |
| `conditions[].conditionSql` | `condition_sql` | `map` | optional | — | A condition that periodically evaluates a SQL query result. |
| `conditions[].conditionSql.booleanTest` | `boolean_test` | `map` | optional | — | Test the boolean value in the indicated column. |
| `conditions[].conditionSql.booleanTest.column` | — | `string` | required | — | Required. The name of the column containing the boolean value. If the value in a row is NULL, that row is ignored. |
| `conditions[].conditionSql.daily` | — | `map` | optional | — | Schedule the query to execute every so many days. |
| `conditions[].conditionSql.daily.executionTime` | `execution_time` | `map` | optional | — | Optional. The time of day (in UTC) at which the query should run. If left unspecified, the server picks an arbitrary time of day and runs the query at the same time each day. |
| `conditions[].conditionSql.daily.executionTime.hours` | — | `integer` | optional | — | Hours of a day in 24 hour format. Must be greater than or equal to 0 and typically must be less than or equal to 23. An API may choose to allow the value "24:00:00" for scenarios like business … |
| `conditions[].conditionSql.daily.executionTime.minutes` | — | `integer` | optional | — | Minutes of an hour. Must be greater than or equal to 0 and less than or equal to 59. |
| `conditions[].conditionSql.daily.executionTime.nanos` | — | `integer` | optional | — | Fractions of seconds, in nanoseconds. Must be greater than or equal to 0 and less than or equal to 999,999,999. |
| `conditions[].conditionSql.daily.executionTime.seconds` | — | `integer` | optional | — | Seconds of a minute. Must be greater than or equal to 0 and typically must be less than or equal to 59. An API may allow the value 60 if it allows leap-seconds. |
| `conditions[].conditionSql.daily.periodicity` | — | `integer` | required | — | Required. The number of days between runs. Must be greater than or equal to 1 day and less than or equal to 31 days. |
| `conditions[].conditionSql.hourly` | — | `map` | optional | — | Schedule the query to execute every so many hours. |
| `conditions[].conditionSql.hourly.minuteOffset` | `minute_offset` | `integer` | optional | — | Optional. The number of minutes after the hour (in UTC) to run the query. Must be greater than or equal to 0 minutes and less than or equal to 59 minutes. If left unspecified, then an arbitrary … |
| `conditions[].conditionSql.hourly.periodicity` | — | `integer` | required | — | Required. The number of hours between runs. Must be greater than or equal to 1 hour and less than or equal to 48 hours. |
| `conditions[].conditionSql.minutes` | — | `map` | optional | — | Schedule the query to execute every so many minutes. |
| `conditions[].conditionSql.minutes.periodicity` | — | `integer` | required | — | Required. Number of minutes between runs. The interval must be greater than or equal to 5 minutes and less than or equal to 1440 minutes. |
| `conditions[].conditionSql.query` | — | `string` | required | — | Required. The Log Analytics SQL query to run, as a string. The query must conform to the required shape. Specifically, the query must not try to filter the input by time. A filter will automatically … |
| `conditions[].conditionSql.rowCountTest` | `row_count_test` | `map` | optional | — | Test the row count against a threshold. |
| `conditions[].conditionSql.rowCountTest.comparison` | — | `string` | required | — | Required. The comparison to apply between the number of rows returned by the query and the threshold. |
| `conditions[].conditionSql.rowCountTest.threshold` | — | `string` | required | — | Required. The value against which to compare the row count. |
| `conditions[].conditionThreshold` | `condition_threshold` | `map` | optional | — | A condition that compares a time series against a threshold. |
| `conditions[].conditionThreshold.aggregations` | — | `list` | optional | same fields as `conditions[].conditionAbsent.aggregations` | Specifies the alignment of data points in individual time series as well as how to combine the retrieved time series together (such as when aggregating multiple streams on each resource to a single … |
| `conditions[].conditionThreshold.comparison` | — | `string` | required | — | The comparison to apply between the time series (indicated by filter and aggregation) and the threshold (indicated by threshold_value). The comparison is applied on each time series, with the time … |
| `conditions[].conditionThreshold.denominatorAggregations` | `denominator_aggregations` | `list` | optional | — | Specifies the alignment of data points in individual time series selected by denominatorFilter as well as how to combine the retrieved time series together (such as when aggregating multiple streams … |
| `conditions[].conditionThreshold.denominatorAggregations[]` | — | `map` | optional | — | — |
| `conditions[].conditionThreshold.denominatorAggregations[].alignmentPeriod` | `alignment_period` | `string` | optional | — | The alignment_period specifies a time interval, in seconds, that is used to divide the data in all the time series into consistent blocks of time. This will be done before the per-series aligner can … |
| `conditions[].conditionThreshold.denominatorAggregations[].crossSeriesReducer` | `cross_series_reducer` | `string` | optional | — | The reduction operation to be used to combine time series into a single time series, where the value of each data point in the resulting series is a function of all the already aligned values in the … |
| `conditions[].conditionThreshold.denominatorAggregations[].groupByFields` | `group_by_fields` | `list` | optional | same fields as `conditions[].conditionAbsent.aggregations[].groupByFields` | The set of fields to preserve when cross_series_reducer is specified. The group_by_fields determine how the time series are partitioned into subsets prior to applying the aggregation operation. Each … |
| `conditions[].conditionThreshold.denominatorAggregations[].perSeriesAligner` | `per_series_aligner` | `string` | optional | — | An Aligner describes how to bring the data points in a single time series into temporal alignment. Except for ALIGN_NONE, all alignments cause all the data points in an alignment_period to be … |
| `conditions[].conditionThreshold.denominatorFilter` | `denominator_filter` | `string` | optional | — | A filter (https://cloud.google.com/monitoring/api/v3/filters) that identifies a time series that should be used as the denominator of a ratio that will be compared with the threshold. If a … |
| `conditions[].conditionThreshold.duration` | — | `string` | required | — | Required. The amount of time that a time series must violate the threshold to be considered failing. Currently, only values that are a multiple of a minute--e.g., 0, 60, 120, or 300 seconds--are … |
| `conditions[].conditionThreshold.evaluationMissingData` | `evaluation_missing_data` | `string` | optional | — | A condition control that determines how metric-threshold conditions are evaluated when data stops arriving. To use this control, the value of the duration field must be greater than or equal to 60 … |
| `conditions[].conditionThreshold.filter` | — | `string` | optional | — | Required. A filter (https://cloud.google.com/monitoring/api/v3/filters) that identifies which time series should be compared with the threshold.The filter is similar to the one that is specified in … |
| `conditions[].conditionThreshold.forecastOptions` | `forecast_options` | `map` | optional | — | When this field is present, the MetricThreshold condition forecasts whether the time series is predicted to violate the threshold within the forecast_horizon. When this field is not set, the … |
| `conditions[].conditionThreshold.forecastOptions.forecastHorizon` | `forecast_horizon` | `string` | required | — | Required. The length of time into the future to forecast whether a time series will violate the threshold. If the predicted value is found to violate the threshold, and the violation is observed in … |
| `conditions[].conditionThreshold.thresholdValue` | `threshold_value` | `float` | optional | — | A value against which to compare the time series. |
| `conditions[].conditionThreshold.trigger` | — | `map` | optional | same fields as `conditions[].conditionMonitoringQueryLanguage.trigger` | The number/percent of time series for which the comparison must hold in order for the condition to trigger. If unspecified, then the condition will trigger if the comparison is true for any of the … |
| `conditions[].displayName` | `display_name` | `string` | required | — | A short name or phrase used to identify the condition in dashboards, notifications, and incidents. To avoid confusion, don't use the same display name for multiple conditions in the same policy. |
| `conditions[].name` | — | `string` | output only | — | Required if the condition exists. The unique resource name for this condition. Its format is: projects/\[PROJECT_ID_OR_NUMBER\]/alertPolicies/\[POLICY_ID\]/conditions/\[CONDITION_ID\] … |
| `creationRecord` | `creation_record` | `map` | output only | — | A read-only record of the creation of the alerting policy. If provided in a call to create or update, this field will be ignored. |
| `creationRecord.mutateTime` | `mutate_time` | `string` | output only | — | When the change occurred. |
| `creationRecord.mutatedBy` | `mutated_by` | `string` | output only | — | The email address of the user making the change. |
| `displayName` | `display_name` | `string` | required | — | A short name or phrase used to identify the policy in dashboards, notifications, and incidents. To avoid confusion, don't use the same display name for multiple policies in the same project. The name … |
| `documentation` | — | `map` | optional | — | Documentation that is included with notifications and incidents related to this policy. Best practice is for the documentation to include information to help responders understand, mitigate … |
| `documentation.content` | — | `string` | optional | — | The body of the documentation, interpreted according to mime_type. The content may not exceed 8,192 Unicode characters and may not exceed more than 10,240 bytes when encoded in UTF-8 format … |
| `documentation.links` | — | `list` | optional | — | Optional. Links to content such as playbooks, repositories, and other resources. This field can contain up to 3 entries. |
| `documentation.links[]` | — | `map` | optional | — | — |
| `documentation.links[].displayName` | `display_name` | `string` | optional | — | A short display name for the link. The display name must not be empty or exceed 63 characters. Example: "playbook". |
| `documentation.links[].url` | — | `string` | optional | — | The url of a webpage. A url can be templatized by using variables in the path or the query parameters. The total length of a URL should not exceed 2083 characters before and after variable expansion … |
| `documentation.mimeType` | `mime_type` | `string` | optional | — | The format of the content field. Presently, only the value "text/markdown" is supported. See Markdown (https://en.wikipedia.org/wiki/Markdown) for more information. |
| `documentation.subject` | — | `string` | optional | — | Optional. The subject line of the notification. The subject line may not exceed 10,240 bytes. In notifications generated by this policy, the contents of the subject line after variable expansion will … |
| `enabled` | — | `boolean` | optional | — | Whether or not the policy is enabled. On write, the default interpretation if unset is that the policy is enabled. On read, clients should not make any assumption about the state if it has not been … |
| `mutationRecord` | `mutation_record` | `map` | optional | — | A read-only record of the most recent change to the alerting policy. If provided in a call to create or update, this field will be ignored. |
| `mutationRecord.mutateTime` | `mutate_time` | `string` | optional | — | When the change occurred. |
| `mutationRecord.mutatedBy` | `mutated_by` | `string` | optional | — | The email address of the user making the change. |
| `name` | — | `string` | output only | — | Identifier. Required if the policy exists. The resource name for this policy. The format is: projects/\[PROJECT_ID_OR_NUMBER\]/alertPolicies/\[ALERT_POLICY_ID\] \[ALERT_POLICY_ID\] is assigned by … |
| `notificationChannels` | `notification_channels` | `list` | optional | — | Identifies the notification channels to which notifications should be sent when incidents are opened or closed or when new violations occur on an already opened incident. Each element of this array … |
| `notificationChannels[]` | — | `string` | optional | — | — |
| `severity` | — | `string` | optional | — | Optional. The severity of an alerting policy indicates how important incidents generated by that policy are. The severity level will be displayed on the Incident detail page and in notifications. |
| `userLabels` | `user_labels` | `map` | optional | opaque | User-supplied key/value data to be used for organizing and identifying the AlertPolicy objects.The field can contain up to 64 entries. Each key and value is limited to 63 Unicode characters or 128 … |
| `validity` | — | `map` | optional | — | Read-only description of how the alerting policy is invalid. This field is only set when the alerting policy is invalid. An invalid alerting policy will not generate incidents. |
| `validity.code` | — | `integer` | optional | — | The status code, which should be an enum value of google.rpc.Code. |
| `validity.details` | — | `list` | optional | — | A list of messages that carry the error details. There is a common set of message types for APIs to use. |
| `validity.details[]` | — | `map` | optional | opaque | — |
| `validity.message` | — | `string` | optional | — | A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client. |
