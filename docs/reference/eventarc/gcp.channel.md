<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.channel`

The Eventarc Channel resource

[eventarc](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | eventarc |
| Scope | regional |
| Asset type | `eventarc.googleapis.com/Channel` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 60s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/locations/{{location}}/channels?channelId={{name}}` |
| Read | yes | `GET projects/{{project}}/locations/{{location}}/channels/{{name}}` |
| Update | yes | `PATCH projects/{{project}}/locations/{{location}}/channels/{{name}}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE projects/{{project}}/locations/{{location}}/channels/{{name}}` |
| Import | yes | by id, see below |

## Import id

```
projects/{{project}}/locations/{{location}}/channels/{{name}}
```

## Attributes

11 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `activationToken` | `activation_token` | `string` | output only | — | Output only. The activation token for the channel. The token must be used by the provider to register the channel for publishing. |
| `createTime` | `create_time` | `string` | output only | — | Output only. The creation time. |
| `cryptoKeyName` | `crypto_key_name` | `string` | optional | — | Optional. Resource name of a KMS crypto key (managed by the user) used to encrypt/decrypt their event data. It must match the pattern `projects/*/locations/*/keyRings/*/cryptoKeys/*`. |
| `labels` | — | `map` | optional | opaque | Optional. Resource labels. |
| `name` | — | `string` | required | force new | Required. The resource name of the channel. Must be unique within the location on the project and must be in `projects/{project}/locations/{location}/channels/{channel_id}` format. |
| `provider_value` | wire `provider` | `string` | optional | force new | The name of the event provider (e.g. Eventarc SaaS partner) associated with the channel. This provider will be granted permissions to publish events to the channel. Format … |
| `pubsubTopic` | `pubsub_topic` | `string` | output only | — | Output only. The name of the Pub/Sub topic created and managed by Eventarc system as a transport for the event delivery. Format: `projects/{project}/topics/{topic_id}`. |
| `satisfiesPzs` | `satisfies_pzs` | `boolean` | output only | — | Output only. Whether or not this Channel satisfies the requirements of physical zone separation |
| `state` | — | `string` | output only | — | Output only. The state of a Channel. |
| `uid` | — | `string` | output only | — | Output only. Server assigned unique identifier for the channel. The value is a UUID4 string and guaranteed to remain unchanged until the resource is deleted. |
| `updateTime` | `update_time` | `string` | output only | — | Output only. The last-modified time. |
