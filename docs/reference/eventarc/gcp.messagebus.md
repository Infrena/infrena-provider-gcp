<!-- GENERATED by cmd/gen-docs from the embedded catalog. Do not edit by hand. -->

# `gcp.messagebus`

The Eventarc MessageBus resource

[eventarc](README.md) · [all services](../README.md)

| | |
| --- | --- |
| Service | eventarc |
| Scope | regional |
| Asset type | `eventarc.googleapis.com/MessageBus` |
| Hangs off | `projects` |
| Tier | 1 (generic-safe) |
| Mutation timeout | 60s |

## What it can do

| Operation | Supported | How |
| --- | --- | --- |
| Create | yes | `POST projects/{{project}}/locations/{{location}}/messageBuses?messageBusId={{message_bus_id}}` |
| Read | yes | `GET projects/{{project}}/locations/{{location}}/messageBuses/{{message_bus_id}}` |
| Update | yes | `PATCH projects/{{project}}/locations/{{location}}/messageBuses/{{message_bus_id}}`, with an update mask naming the changed fields |
| Delete | yes | `DELETE projects/{{project}}/locations/{{location}}/messageBuses/{{message_bus_id}}` |
| Import | yes | by id, see below |

## Import id

```
projects/{{project}}/locations/{{location}}/messageBuses/{{message_bus_id}}
```

## Attributes

12 attribute(s), at every depth. A `.` is a field of an object; `[]` is the element of a list.

| Attribute | Also spelled | Type | Set by | Notes | Description |
| --- | --- | --- | --- | --- | --- |
| `annotations` | — | `map` | optional | opaque | Optional. Resource annotations. |
| `createTime` | `create_time` | `string` | output only | — | Output only. The creation time. |
| `cryptoKeyName` | `crypto_key_name` | `string` | optional | — | Optional. Resource name of a KMS crypto key (managed by the user) used to encrypt/decrypt their event data. It must match the pattern `projects/*/locations/*/keyRings/*/cryptoKeys/*`. |
| `displayName` | `display_name` | `string` | optional | — | Optional. Resource display name. |
| `etag` | — | `string` | output only | — | Output only. This checksum is computed by the server based on the value of other fields, and might be sent only on update and delete requests to ensure that the client has an up-to-date value before … |
| `labels` | — | `map` | optional | opaque | Optional. Resource labels. |
| `loggingConfig` | `logging_config` | `map` | optional | — | Optional. Config to control Platform logging for the Message Bus. This log configuration is applied to the Message Bus itself, and all the Enrollments attached to it. |
| `loggingConfig.logSeverity` | `log_severity` | `string` | optional | — | Optional. The minimum severity of logs that will be sent to Stackdriver/Platform Telemetry. Logs at severitiy ≥ this value will be sent, unless it is NONE. |
| `message_bus_id` | — | `string` | required | force new | The message_bus_id this gcp.messagebus belongs to. It is part of the resource's name, supplied in the create url rather than in the request body. |
| `name` | — | `string` | output only | — | Identifier. Resource name of the form projects/{project}/locations/{location}/messageBuses/{message_bus} |
| `uid` | — | `string` | output only | — | Output only. Server assigned unique identifier for the channel. The value is a UUID4 string and guaranteed to remain unchanged until the resource is deleted. |
| `updateTime` | `update_time` | `string` | output only | — | Output only. The last-modified time. |
